# 洛书 v2.5.0

v2.5.0 把洛书继续完善为更完整的无 Hook 全局字体工作台，同时保持现有字体替换核心与 Play/GMS 字体链路不重写。

## 新功能
- 完整设置中心：总览、外观、安全与更新统一管理。
- 安全中心：Root、挂载、字体负载、切换锁、缓存、重启状态和其它字体模块冲突检查。
- 稳定版 / 预发行更新通道与发布校验信息。
- 本地字体方案库：保存、收藏、重命名、删除和一键载入中文/英文/数字组合。
- 完整备份 / 恢复：用户字体、收藏标签、字体方案、Studio、外观和可安全迁移配置，并校验 SHA-256。
- 实际成功切换历史与一键回退；恢复继续走原有事务和安全校验。
- A/B 预览扩展到混排、正文、界面、微信、Play 商店、状态栏数字、金额、小字号和粗体标题。
- 缺字地图扩展到简繁中文、日文假名、韩文、Latin Extended、数字、标点、数学符号与 PUA。

## 稳定性
- 修复 `.font_switch.lock` 目录残留后跨重启无法恢复的问题。
- 开机和模块升级改用专用锁清理，两个复合入口、service、字体缓存和开机验证按活持有者判断忙碌。
- `busy` 查询保留锁初始化宽限，避免 mkdir 成功但 owner metadata 尚未写入时出现并发误判。
- 新增残留锁恢复、初始化竞态以及 weighted / multiweight 两条复合入口端到端回归测试。
- 继续保留 PID + process starttime + boot_id 身份锁与旧 PID-only 锁兼容。

## 发布说明
- 后端专项、Android 单元测试、Lint 和 APK assemble 纳入 CI；正式发布继续构建签名 APK、模块 ZIP、SHA-256 并回下载校验。
- ColorOS 16、HyperOS 3、Magisk 与 APatch 真机矩阵仍记录为 pending；本次正式发布基于维护者明确授权，不伪造任何真机通过结果。

版本：`v2.5.0`  
模块 versionCode：`20500`  
App versionCode：`2050001`
