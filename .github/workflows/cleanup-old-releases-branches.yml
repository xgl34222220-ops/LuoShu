name: Cleanup old releases and branches

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo cleanup finished; workflow retained as manual-only placeholder
