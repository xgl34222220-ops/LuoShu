name: Cleanup old releases and branches

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo cleanup workflow disabled after execution
