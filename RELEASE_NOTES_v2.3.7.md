<!-- prerelease -->
# 洛书 v2.3.7

v2.3.7 是可靠性预发行版。它包含 v2.3.6 正式版之后的本次启动字体激活证据修复，并集中解决 APatch 安装提交、App 常驻轮询、状态误导和发布签名门禁。

## 主要修复

- 字体文件与配置仍作为同一个原子事务提交；任一组件失败都会逆序卸载并回到系统字体，不允许半挂载。
- 只有当前 `bootId` 的字体事务、自挂载清单和动态配置状态同时成立，并且 PID 1 视角确认 `mounted`，App 才显示“本次启动字体已验证”。
- 修复 APatch 以 `source` 方式运行安装脚本时，`exit 0` 提前结束父安装器、重启后模块文件消失的问题；加入 source 安装回归测试。
- App 与字体导入前台服务改为状态变化驱动，不再每 250ms 常驻读取任务状态；前台服务不再使用粘性重启。
- 高刷新率默认跟随系统。用户主动开启后，洛书只请求同分辨率高刷新率，并在系统省电模式下自动停止请求。
- 首页和设备可信状态明确区分“字体已准备”“等待完整重启”“本次启动字体已验证”与失败原因，不再把过期启动记录或兼容映射当作成功。
- 自挂载验证额外核对 PID 1 `mountinfo`；逐文件 bind 同时核对源文件和系统可见目标的设备号与 inode。

## 发布安全

- APK 必须只有一个 signer，证书 SHA-256 必须与 v2.3.6 正式版一致。
- 本版本只发布为 GitHub prerelease。
- ColorOS 16 + KernelSU、HyperOS 3 + KernelSU、通用 Magisk、通用 APatch 的真机验收证据尚未全部完成，因此不得标记为稳定版。
- 已有 v2.3.6 Tag 和 Release 保持不变，不移动、不覆盖。

## 重点真机验证

1. 一加 11 / ColorOS / APatch 刷入后确认模块目录在重启后仍存在。
2. 应用字体并完整重启，确认首页只在本次启动 PID 1 可见且 `mounted` 时显示验证成功。
3. 模拟任一字体或 XML 挂载失败，确认所有洛书挂载逆序回滚并恢复系统字体。
4. 验证导入完成、暂停、恢复和系统回收后任务状态正确，空闲时不再持续唤醒。
5. 卸载后确认洛书挂载、私有负载和 App 状态均被清理。

## 版本信息

- 模块版本：`v2.3.7`
- 模块 versionCode：`20307`
- App versionName：`2.3.7`
- App versionCode：`2030701`
- 发布类型：预发行版
