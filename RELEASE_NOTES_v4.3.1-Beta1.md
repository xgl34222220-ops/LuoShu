<!-- prerelease -->
# 洛书 v4.3.1-Beta1

HyperOS 英文、数字覆盖回归修复预发行版，基于 v4.3.0。按用户要求合并 PR #223 后发布，用于 K80 至尊版等设备实测；不替代 v4.3.0 正式渠道。

## 本次修改

- 补回 4.3.0 收窄白名单时遗漏的 NotoSansMono、Display、Condensed、部分 VF，以及 DroidSansMono / DroidSansFallback 等明确 UI 字体入口。扫描、最终生成与开机映射规则同步。
- 修复 SemiCondensed 名称包含 icon 子串而被误判为图标字体的问题；真正图标、Emoji、无关语言、斜体和 TTC 仍按原有策略保护。
- HyperOS 覆盖清单修订升级到 4，旧清单通过现有可信原厂路径重新扫描，防止沿用漏项清单；ColorOS 不因此被强制重扫。
- 保留 4.3.0 的字体子集压缩、生成结果复用、Google 下载字体维护及主题动态链接，不恢复全目录无差别复制。
- 补充恢复入口的策略及原厂扫描回归测试；更新旧测试中与新覆盖策略冲突的 DroidSansMono 排除断言。

## 安装与测试

**默认卸载模块一定要关闭。** 测试前保留可用的正式版安装包。

1. 覆盖安装本页模块 ZIP 与配套 APK，先完整重启，让新扫描规则读取原厂字体。模块内也包含同一签名的配套 App。
2. 在洛书中重新应用一次当前字体或组合，再按提示完整重启。无需删除原字体或重新导入；仅安装新版、不重新应用，旧生成字体不会自动变成新规则结果。
3. 对比原来漏覆盖的英文和数字页面；继续检查 Chrome / Google 应用放置后是否恢复默认，以及再次应用和连续重启后的效果。
4. 检查模块实际占用是否异常增加，同时注意等宽、窄体页面是否出现新的偏移或裁切。

出现明显异常时可停用模块并重启恢复系统字体，再回到保留的可用版本。不要手动删除活动字体目录。

## 验证边界

这是用于真机确认的预发行版，不宣称已在 K80 至尊版或一加 15 上验证全部覆盖、实际占用和重启稳定性。发布流程执行源码回归、Android lint / 单元测试、签名校验和模块打包检查；其结果不等于真机渲染验收。测试夹具名称不是本次读取的用户手机字体清单。应用内置字体与网页明确指定的网络字体不等同于系统字体入口。
