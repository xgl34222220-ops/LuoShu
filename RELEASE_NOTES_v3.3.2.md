# 洛书 v3.3.2

这是针对 v3.3.1 循环重启问题的安全修复版本。

## 修复内容

- 修复活动字体负载处于“等待后台重建”时，`post-mount` 仍绕过开机守卫挂载旧负载的问题。
- 只有开机守卫已授权为 `booting` 或此前已确认 `confirmed` 的负载才允许进入系统字体目录。
- 待重建、状态缺失或未准备完成的负载全部延后挂载，设备先使用 ROM 原字体完成启动。
- 安全门禁命中时会记录 `deferred`，不会再触碰 `/system/fonts`。
- 更新到 v3.3.2 时，v3.3.1 的活动负载会按新的 boot-safe 架构在开机后重建；重建成功后再按通知完整重启一次加载字体。
- 保留 v3.3.1 对 ColorOS / HyperOS 缺失可选字体分区的兼容修复。
- 发布门禁使用独立存活进程验证锁身份，避免 CI 启动器 PID 不可见造成误报。

- 修复发布源码中的字体安全脚本完整性，发布前会由完整检查阻断异常脚本。

## 升级行为

从 v3.3.1 更新后的第一次开机使用系统原字体是预期的安全行为。请等待洛书提示后台重建完成，再完整重启一次；不要在重建完成前反复重启。
