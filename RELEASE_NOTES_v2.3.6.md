# 洛书 v2.3.6

v2.3.6 是后台任务与开机性能修复正式版。它解决字体切换完成并重启后仍出现异常任务、高 CPU 或“进程异常”的问题，同时保留洛书无 Hook、私有自挂载和事务回滚架构。

## 主要修复

- 后台任务 PID 增加 Android `bootId` 绑定，完整重启后不再把复用的 PID 误认成上一启动周期的任务。
- 字体切换任务增加持续心跳、统一超时状态和更完整的 App 恢复窗口，大型 CJK、TTC、CFF/CFF2 与复杂可变字体不再被过早判定为异常。
- 字体库 `value` / `fingerprint` 查询恢复为纯读取，不再在每次开机隐式启动 Python 槽位规划进程。
- 槽位规划改为显式维护任务，加入默认 180 秒硬超时、低 CPU 优先级、轻量文件身份缓存、双重缓存检查和跨启动周期陈旧锁清理。
- 停止 `post-fs-data` 自动调度深度字体加载验证，避免重启后重复遍历、哈希完整字体树并启动 Python。
- 正常开机只根据已确认的字体事务与自挂载状态记录当前字体生效，状态更新为常量时间操作。
- 原 FontManager、可见字体指纹与 Python 深度验证仍保留为手动诊断命令，不影响正常换字体流程。
- 修复任务中心诊断与导出按钮图标对齐，并完善 App 自动安装的瞬时错误重试与永久错误区分。

## 正常使用

1. 在 Magisk、KernelSU、SukiSU Ultra 或 APatch 中直接刷入模块 ZIP。
2. 完整重启设备。
3. 首次打开洛书时允许 Root 授权。
4. 选择单字体或组合字体，任务完成后按提示完整重启。

正常开机不会再自动运行深度加载验证。需要排查字体挂载或 FontManager 证据时，可手动执行：

```sh
su -c 'sh /data/adb/modules/LuoShu/common/device_font_load_verify.sh verify'
```

模块 ZIP 已内置同版本正式签名 App，也可以单独安装发布页中的 APK。覆盖升级会保留字体库、组合方案和现有配置。

## 安全约束

- 不修改只读系统分区中的原始字体文件。
- 字体负载事务提交前仍执行必要的结构、角色、配置与自挂载校验。
- 挂载失败时完整回滚并安全使用系统默认字体，不保留混合字体树。
- 取消的是开机后的重复深度扫描，不是字体切换事务和自挂载安全检查。
- 卸载模块后可恢复系统原始字体状态。

## 版本信息

- 模块版本：`v2.3.6`
- 模块 versionCode：`20306`
- App versionName：`2.3.6`
- App versionCode：`2030601`
- 发布类型：正式稳定版
