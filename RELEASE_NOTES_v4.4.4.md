# 洛书 v4.4.4 正式版

按维护者要求发布正式版，汇总 v4.3.1-Beta1 与 v4.3.2-Beta1 的修复。字体运行代码与 v4.3.2-Beta1 一致，本次统一正式版本、更新通道与发布规则，不另外加入未经测试的字体行为变更。

## HyperOS 英文与数字覆盖

- 补回此前白名单遗漏的 NotoSans Mono / Display / Condensed / 部分 VF，以及 DroidSansMono / DroidSansFallback 等明确 UI 字体入口；扫描、生成和开机映射保持一致。
- 修复 SemiCondensed 被 icon 子串误判的问题，继续保护真正的图标、Emoji、无关语言、斜体及不安全 TTC。
- 覆盖清单修订更新到 4，旧清单走可信原厂重扫；保留字体子集精简及生成结果复用，不恢复全目录大体积复制。

## Google 无 Hook 字体守护

- 恢复默认字体后守护暂停而不永久退出，同次开机内重新选择字体会恢复维护。
- 已知字体缓存目录的创建、写入完成、替换、删除会唤醒检查；保留 30 秒元数据复查与观察器不可用时的定时回退。
- 等待期间检查 Google / Chrome 进程身份，补上共享挂载环境中新进程及 PID 复用漏检。
- 保留后台旧字体刷新、前台应用保护、停止等待与锁释放；不加入 Hook，不停用 GMS，不封禁 Google 联网。

## 升级

**“默认卸载模块”一定要关闭。**

覆盖安装本页模块 ZIP 和配套 App，随后完整重启。模块已内置同一签名 App，必要时使用独立 APK 覆盖安装。

已经在 v4.3.1-Beta1 或 v4.3.2-Beta1 按新规则重新应用过字体的，本次不要求重新生成。从 v4.3.0 或更早版本升级、尚未重建过字体的，先重启刷新原厂清单，再重新应用当前字体或组合，并按提示完整重启。无需删除或重新导入原字体。

## 发布与版本规则

模块 versionCode 为 40404，App 为 4040401。正式及预览更新元数据同步到本版。按照维护者指定顺序，后续正式版为 **5.0.0 → 5.5.5 → 6.0.0 → 6.6.6**，规则记录在 `config/stable_version_policy.json` 和 `docs/RELEASING.md`。

新包及更新元数据就绪后，清理发布前最近六个版本中的五个：v4.3.2-Beta1、v4.3.1-Beta1、v4.3.0、v4.2.0、v4.1.0；**v4.0.0 保留**。同时删除本次授权时已经发布的全部预发行版。更早正式版不在本次清理范围；只删除 Release 及附件，不删除源码标签和提交历史。

## 验证范围

维护者明确授权本次正式发布；尚未完成的 K80 至尊版 / 一加 15 长期真机覆盖、功耗与稳定性项目仍保持待测，不伪造通过记录。事件监听不是字体加载前的权限拦截，已缓存旧字体仍可能需要后台刷新；不能据此承诺所有 Google 页面永不回退。安装包沿用固定证书签名、源码回归、Android lint / 单元测试、模块完整性及 SHA-256 校验流程。
