# 洛书 v2.5.4

本次正式版集中精修字体库视觉与预览体验，不改变字体引擎、provider bridge、挂载、事务回滚或字体组合算法。

## 字体库精修
- 所有字体卡预览统一为 `Aa12`，系统默认、有效字体和异常 fallback 不再混用 `Aa` / `Aa 12`。
- 有效字体继续使用字体自身渲染预览，固定预览盒尺寸与基线，列表滚动时视觉更整齐。
- 字体卡重新组织字体名称、格式/大小/日期、字重能力和覆盖标签，减少重复说明文字与“文件管理器”感。
- `应用` 按钮缩为紧凑主操作，当前字体使用独立 `使用中` 状态；删除继续收进更多菜单。
- 顶部统计、搜索、排序和管理入口进一步简化，减少工程信息堆叠。

## 字体详情
- 字体详情从居中的 `AlertDialog` 升级为大尺寸 `ModalBottomSheet`。
- 新增 `综合 / 中文 / English / 数字` 四类真实字体预览，其中仅在字体支持 CJK 时显示中文预览。
- 综合预览固定展示中文、英文和数字样张，方便快速判断字体实际观感。
- 常用字体能力、字重、覆盖、格式、大小和导入时间集中到精简信息卡。
- 内部名称、SHA-256、字形覆盖和可变轴等技术元数据默认折叠，需要时再展开。
- 当前使用字体显示清晰状态，未使用字体在详情底部提供统一的“应用此字体”主操作。

## 回归保护
- 更新字体库 UI 回归契约，强制 `Aa12` 统一预览并禁止旧 `Aa 12` 和重复说明文案回流。
- 回归契约要求字体详情保持 Bottom Sheet、大预览和多场景预览模式。

## 验证
正式候选和发布链执行：
- `scripts/check.sh`
- Android `lintDebug`
- Android `testDebugUnitTest`
- Android `assembleDebug`
- 正式 Release 链再次执行完整源码/模块检查、Release Lint、单元测试、正式签名 APK 和发布就绪门禁。

## 真机状态
ColorOS 16、HyperOS 3、Magisk、APatch 真机矩阵仍保持 pending；本次稳定版由维护者明确授权放行，不伪造真机 PASS。
