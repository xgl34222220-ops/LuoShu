# 洛书 v2.2.6

洛书 v2.2.6 是针对 v2.2.5 真机反馈发布的稳定性修复版本，重点修正验收状态误导、ROM 检测日志重复和后台设备对齐缓存未完整接入元模块同步的问题。

## 验收状态

- 真机验收向导区分“通过、等待、兼容、失败”四种状态。
- 兼容字体映射属于正常可用状态，不再把“字体加载模式”和“设备对齐缓存”长期显示为红色失败。
- 字体任务执行中、等待完整重启和后台缓存生成均显示为信息状态，不再诱导用户反复重启。
- 只有逐分区加载验证明确失败或可信状态读取异常时才显示红色阻断，并引导前往问题页查看失败分区。
- 验收摘要同步区分“通过、等待、失败”，避免把正常后台处理误写成故障。

## ROM 日志去重

- HyperOS / MIUI 与 ColorOS 版本只在首次识别或版本发生变化时写入一次日志。
- 升级后首次初始化会自动压缩 `fontswitch.log` 中已有的相同 ROM 检测记录。
- 首页刷新、字体库扫描和组合任务仍可重复读取 ROM 信息，但普通查询不再污染日志。

## 后台设备对齐缓存

- 独立后台缓存任务现在会显式加载设备字体事务守卫与 `mount_compat.sh`。
- 缓存生成完成后会同步到当前实际使用的 Magic Mount、meta-overlayfs、Hybrid Mount 或 Mountify 后端。
- 同步与事务函数缺失时不再静默完成，避免缓存已经生成但真实挂载负载未更新。

## 安装与升级

1. 在 Magisk、KernelSU、SukiSU Ultra 或 APatch 中刷入 `LuoShu-v2.2.6.zip`。
2. 完整重启设备并打开洛书授予 Root 权限。
3. 原有字体已经正常生效时无需反复重新应用或多次重启；进入验收向导点击“重新检测”即可。
4. 升级后首次打开日志页时，旧的重复 HyperOS / ColorOS 检测记录会自动整理。

模块 ZIP 内置同版本正式签名 App。独立的 `LuoShu-App-v2.2.6.apk` 可用于单独安装或覆盖升级，但仅安装 APK 不会更新后台对齐同步和 ROM 日志整理逻辑。

## 版本信息

- 模块版本：`v2.2.6`
- 模块 versionCode：`20206`
- App versionCode：`2020601`
- 最低 Android：Android 9 / API 28
- 目标 Android：API 36

## 校验

Release 同时提供模块 ZIP、正式签名 APK 及各自的 SHA-256 文件。下载后可使用对应 `.sha256` 文件核验完整性。
