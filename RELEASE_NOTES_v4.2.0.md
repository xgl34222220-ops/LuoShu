# 洛书 v4.2.0 正式版

本版重做 App 的主要界面，修复字体任务、后台维护和字体缓存中的实际问题，整合 4.1.0 之后的字体兼容修复。

## 界面与操作

- 首页以当前字体预览和主要操作为中心，设备及扫描详情按需展开。
- 字体库使用独立预览卡片，排序可直接选择，搜索和空列表提供明确操作。
- 组合工作室简化槽位与预览层级，减少重复的原生字体预览，优先呈现应用按钮。
- 设置按用途分组，外观选项即时预览；日志支持筛选、搜索与复制。
- 统一字号、圆角、间距和触控区域，改善深色模式、玻璃浮动导航及底部内容遮挡。

## 后台与字体切换

- 减少 Google 字体开机窗口重复启动 Python 和计算大字体摘要；稳定时以轻量巡检为主。
- 修复任务取消、超时清理和 App 状态轮询，避免请求叠加与实际等待被查询耗时拖长。
- App 进入后台暂停任务状态查询，已分离的字体处理任务继续运行。
- 清理 HyperOS 处理完成后的临时字体，避免重复操作累计无用文件。

## 字体兼容

- 修复组合字体替换字形后仍继承基底 HVAR/VVAR 增量的问题，避免导入的静态英数随基底字重产生额外度量变化。
- 保留 K80 原厂动态字体入口和主题切换路径，按实际物理槽处理原厂度量。
- 补齐 Google 下载字体开机后维护，缓存变化时重新处理，减少维护过程对应用的打扰。

## 安装与已知范围

1. 在 Root 管理器安装 `LuoShu-v4.2.0.zip`，按提示完整重启。
2. 打开洛书，重新应用一次所需字体或组合，再完整重启，使新生成的字体与缓存生效。

模块版本代码 **40200**，App 版本代码 **4020001**。正式 App 使用原有签名；模块内置同一 APK。默认卸载模块选项保持关闭。

K80 空字体主题会改变系统选字路径，但不代表所有应用偏移已消除。本版没有把 K80/一加的未完成真机复测标记为通过。应用内置的秒表专用字体、Google 应用已经加载到内存的 Typeface 等，仍可能不随系统字体即时替换；本版不承诺所有应用完全覆盖或所有手机固定时间内完成切换。
