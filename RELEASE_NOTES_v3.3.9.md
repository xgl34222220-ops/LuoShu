# 洛书 v3.3.9 真机验收候选

这一版只重做被指出不符合参考软件的悬浮底栏，同时保留 v3.3.8 的字体切换修复。底栏不再是“透明底板加一个蓝色色块”，而是按参考录屏所用的 Miuix 引用栈接入真实背景取样、模糊、折射和连续圆角。

## 真液体玻璃底栏

- Android 13 及以上使用 Miuix `blur`、`shader` 与 `squircle`：页面先作为背景源，外壳对页面取样并折射，选中透镜再对外壳取样。
- 透镜切换时按方向拉长、压缩后弹性回弹，边缘有克制的高光与色散；图标、中文标签始终作为最上层清晰内容绘制。
- 外壳保持约 14 dp 横向悬浮边距、52 dp 高度，内部项目 44 dp，不放大成占屏的白色导航块。
- Android 12 及以下无法运行 RuntimeShader，自动使用只包裹背景节点的 Haze 降级，不牺牲最低系统兼容性。

## 字体切换保持不变

- 保留一次应用只提交一个原子负载、一次完整重启生效的路径。
- 保留 HyperOS 的 MiSans、MiSansLatin、Roboto、GoogleSans、Mitype、数字字重、时钟与 `mi_ext` 字体槽覆盖。
- 前台切换不运行 Python、不生成九份字体、不安排成功后的后台二次改写。

## 版本与验收

- 模块：v3.3.9（30309）
- App：v3.3.9（3030901）

请在 HyperOS 真机重点检查：底栏下方页面内容能够真实透过并随页面变化，选中透镜移动时存在折射与回弹，图标文字后没有白块；然后连续切换两款字体，确认每次只需应用一次并完整重启一次，外部 App 的中文、英文和数字都使用所选字体。
