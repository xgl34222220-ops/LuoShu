# 洛书 v4.0.0 正式版

本版重新发布 v4.0.0，集中修复 ColorOS 字体切换闪退/任务锁死、模块更新后字体恢复默认、HyperOS 大量系统 UI 仍使用原字体，以及重启后长期显示“已准备，待本次启动验证”等真机回归。App 与模块版本号保持 v4.0.0，不另起版本号。

## 字体切换与更新

- 字体切换改为“下一启动负载”事务：点击切换时绝不删除、改写或重命名本次启动正在使用的 `.luoshu-payload`，只在独立暂存区生成并校验新字体负载。
- 校验完成后保存为 `.luoshu-payload-next`；只有下一次早期开机、系统字体尚未挂载前才原子激活，避免 ColorOS/SystemUI/App 因运行中的字体源被改动而闪退。
- 激活器保留上一套负载用于失败回退；新负载未完整准备成功时不会破坏当前可用字体。
- 更新已有 v4.0.0 时显式继承正在使用的 legacy 物理字体负载，不再因 schema 误判把当前字体当成旧缓存清理，从而避免更新模块后恢复系统默认字体。
- 字体任务守卫同时校验 PID、task sidecar、Android boot ID 与 cmdline；进程已死、PID 被复用或跨启动遗留的任务会自动释放，不再永久提示“已有字体任务在运行中”。
- 切换阶段会持久化真实百分比和当前步骤，App 被系统杀掉或重新打开后仍可恢复任务进度显示。
- 安装/更新过程不在 Root 管理器刷写界面同步重建大字体，避免再次出现 94% 长时间卡住或刷写阶段自毁当前负载。
- KernelSU / SukiSU / APatch 等环境继续使用私有负载投影视图和自挂载兼容路径，安装器退出与清理逻辑彼此隔离。

## HyperOS 全局覆盖

- 根据真机实际存在的物理字体槽动态补齐覆盖，不再只处理状态栏和锁屏时钟。
- 覆盖范围包括设备实际存在的 `system`、`system_ext`、`product`、`mi_ext`、`vendor`、`odm`、`oem`、`my_product`、`hw_product` 与 `cust` 字体目录。
- 扩展 MiSans、XiaomiSans、MiLanPro、Mitype、MiClock、Roboto、Google Sans / Google Sans Text、Noto Sans UI、Source Sans Pro、Droid Sans 与 100–900 数字字重等普通系统 UI 槽。
- 覆盖 HyperOS 3 状态栏时间、锁屏时钟、控制中心及部分设置/安装器/Launcher/应用英文数字直连槽，包括 `MitypeClock`、`MiClock`、`MiSansClock`、`AndroidClock`、`Clockopia` 等。
- 动态扩展只处理安全的 upright TTF/OTF UI 字体；TTC 容器、Emoji、图标、衬线、斜体及阿拉伯/日韩等语言专用字体保持原系统，避免为了覆盖率引入 face-index 或缺字崩溃风险。
- HyperOS 补槽直接复用已经生成好的选中字体负载，不重新进入 device-template / XML / 94% 重建链。

## ColorOS

- 修复切换字体时直接清空运行中挂载源导致 App 闪退、重新进入后任务锁死的问题。
- 修复更新洛书后当前字体负载被错误迁移/清理而恢复默认字体的问题。
- 当前启动继续使用原负载直到完整重启；新字体只在下一启动早期接管，降低 SystemUI、Launcher 和应用进程读取到半成品字体树的风险。

## 状态与验证

- 修复 legacy 物理映射已经完成启动验证却仍显示“已准备，待本次启动验证”的状态判定错误。
- 不再无条件伪造 `verified`：只有自挂载状态真实为 mounted/degraded/confirmed 时才确认成功，实际挂载失败会明确显示失败。
- 切换完成仅表示“下一启动负载已准备”；完整重启并完成真实挂载后才进入已验证状态。

## 界面

- 页面切换只绘制目标页面的入场帧，不再叠加上一页退出动画，修复切页瞬间闪回旧页面。
- 悬浮底栏采用背景取样、折射外壳、移动透镜和独立内容层；图标与文字保持在模糊/着色器之外。
- 右上角刷新/工具按钮统一视觉尺寸和 48 dp 触控区；更新页下载入口、标题与 SHA-256 保持完整显示。

## 版本

- 模块：v4.0.0（40000）
- App：v4.0.0（4000001）

## 发布说明

- 已通过字体引擎 Smoke、设备字体清单、预发布检查、更新负载保留、94% 有界验证和 Shell 语法门禁；正式发布工作流会再次执行完整源码检查、Android Lint/单元测试、签名 APK、模块打包及候选产物校验。
- 本次正式包对应 PR #210 及其后续发布门禁修正，不再提供此前指向旧提交的 v4.0.0 产物。
- 发布入口兼容契约已同步恢复，确保模块启用状态处理继续通过完整挂载兼容回归门禁。
