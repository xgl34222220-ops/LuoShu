# 洛书 v3.3.3

这是针对 v3.3.0 的正式修复版，重点修复字体切换失败并收紧新版界面密度。

## 字体切换修复

- 修复 KernelSU 等环境下，可选的 `system_ext/fonts`、`product/fonts` 等分区或目标不存在时被误判为致命错误，导致整个字体挂载事务回滚的问题。
- `system/fonts` 仍是必须成功挂载的核心目标；其余 OEM/可选字体目标缺失时安全跳过，不再连带撤销已成功的系统字体挂载。
- App 状态桥接会保留并展示真实失败目标，任务中心不再只显示笼统的 `mount-active-visible-layout-differs`。
- 增加原子自挂载、运行时挂载、状态桥接与字体任务回归测试。

## 界面调整

- 缩小首页、字体库、任务中心和设置中心的页标题与正文层级。
- 收紧卡片圆角、内边距、区块间距、图标容器和按钮高度。
- 缩小底部导航高度和图标，使同屏信息更多、操作区域更协调。

## 版本

- 模块：v3.3.3（30303）
- App：v3.3.3（3030301）

从 v3.3.0 更新后请完整重启一次，再重新选择并应用字体。
