# 洛书·重构版 2.0.0 正式版

2.0.0 重点完成「字体覆盖中心」和真实补齐链路，并继续收紧系统字体扫描、OEM 嵌套目录挂载、组合任务状态与下一启动原子提交。

## 字体覆盖中心

- App 新增「字体覆盖」页面，直接查看每个真实字体槽位的路径、分区、字重、来源、映射与当前状态。
- 状态区分已替换、待验证、系统保护、异常与可安全补齐，不再只看刷写日志猜覆盖情况。
- 支持按状态 / 分区筛选、搜索、重新验证与导出诊断报告。
- 刷写阶段与 App 共用同一套设备 inventory / candidate census 口径，避免“刷写页槽位数”和 App 各算各的。

## 一键安全补齐

- App 会把本次异常槽位生成精确 remediation plan，并把真实 taskId 交给任务跟踪器。
- 补齐过程中实时显示阶段、进度与错误；任务完成后直接进入「完整重启」状态。
- 新 stage 不只修单个红项，同时回填所有安全缺失槽，避免修一个槽时丢掉原本正常的覆盖。
- Emoji、图标、数学 / 音乐符号、高风险 TTC / OTC collection、特殊 fallback 与缺失真实字重的槽位继续保持原厂。
- 下一启动 payload 与状态文件原子提交；完整重启一次后按真实挂载与加载证据重新验证。

## OEM / ROM 覆盖

- 扫描器发现的 OEM 嵌套字体根会真正进入 Overlay / Bind 挂载，例如厂商自定义的 `/product/.../fonts`。
- 修复“扫描到了、payload 里也有文件，但 Android 实际没有挂载”的覆盖缺口。
- HyperOS / ColorOS 继续使用本机原厂 metrics 对齐，不按机型白名单硬编码。

## 组合任务与稳定性

- 修复 Android PID 复用后，旧 `axes_worker.pid` 等残留误报“已有字体组合任务正在运行”。
- 组合任务改为 PID + taskId + bootId + cmdline 联合验证，失效 sidecar 自动释放。
- 修复兼容运行时的 `config / logs / system/fonts` 为软链接目录时，`mkdir -p` 误报“无法创建模块工作目录”。
- 组合生成结束后必须等下一启动 payload 真正提交成功才进入 100%，finalizer 被系统回收时可自动恢复提交。
- generation manifest 与 requestId 绑定，避免上一轮组合状态误提交到新任务。
- finalize 进度按 taskId 严格隔离，旧任务或空 task 的 90/96/98% 状态不会污染新任务进度。

## 补齐末段性能

- 把 ROM 槽位度量与批量补齐从最后 99% 前移到 90–98%。
- HyperOS / ColorOS 已经完成本机 metrics 归一化的槽位，不再在补齐阶段重复跑 FontTools。
- 98% 完成预提交处理后，99% 只保留轻量原子 rename 与状态落盘，减少“99% 长时间不动”的等待感。

## 安装与升级

**Root 管理器中的「默认卸载模块」必须保持关闭。**

从重构版 1.0.0 / 1.1.1 或旧系列升级，可直接刷入 2.0.0 模块并覆盖安装 App，完成后完整重启。已有字体与用户配置继续按升级迁移逻辑保留。

本版模块 versionCode 为 **70000**，App versionCode 为 **7000001**。下一正式版本为 **2.2.2**。
