version=v13.5 Stable Hotfix4
changes=按 Hybrid Mount 公开源码中的真实扫描规则处理模块根目录 .sh：正常 mkdir/touch 初始化改用 command 前缀，不再误报“包含挂载相关命令”；文字字体与 Emoji 的大量 ROM 别名改为符号链接，避免 ext4 staging 把同一字体复制几十次导致 No space left on device。
font_path=/sdcard/LuoShu/fonts/
emoji_path=/sdcard/LuoShu/emoji/
import_path=/sdcard/LuoShu/import/
hybrid_mount=Hotfix4 可使用 Overlay 或 Magic；首次升级后请先清除 Hybrid Mount 旧挂载错误并重新加载。若旧 ext4 staging 已被占满，可切换一次 tmpfs 或重建 Hybrid Mount 存储后再同步。
