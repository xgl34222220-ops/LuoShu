version=v14.3 Alpha1.9
summary=双皮肤弹层统一与旧 UI 清理
notes=应用、删除、恢复系统字体、组合字体选择、字体详情、安全导入结果和分析进度均提供独立 Material 与 Miuix 表现；导入浮层已移入全局主题并只在字体库/组合页显示。运行入口收敛到 LuoShuAppShell，删除 Alpha1.5、Alpha1.6、旧单皮肤、共享过渡页、Stage2 临时壳和旧底栏作用域。DataStore 外观数据增加自动归一化与 JVM 回归测试。Build module 29679747456 与 Validate source 29679747462 全部通过。字体切换、复合生成、可变轴、静态多字重、覆盖诊断和 Root 逻辑保持不变。
