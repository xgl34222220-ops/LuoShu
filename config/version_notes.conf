version=v3.3.1
summary=洛书 3.3.1：修复 ColorOS / HyperOS 可选字体分区缺失导致整套挂载回滚
notes=v3.3.1 修复 KernelSU / SukiSU / APatch 私有 payload 自挂载回归：只有 system/fonts 是强制目标，product、system_ext、mi_ext 等跨 ROM 可选分区或字体目录不存在时安全跳过，不再触发 mount-active-visible-layout-differs 和默认字体回退。同步覆盖基础原子层、post-mount 最终层与私有 payload 运行层。模块 versionCode 30301，App versionCode 3030101。
