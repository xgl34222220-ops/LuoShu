version=v14.1 Test3
changes=字体组合记录精确源路径与 SHA-256，未修改槽位不再按家族名重新猜文件；英文与数字槽应用前检测字符覆盖和中文抢占风险；ColorOS 每次启动先清理洛书管理的旧 /data/fonts 映射；字体预览改为按需生成并最多缓存 3 个家族，不再把整个字体库复制进模块；新增真实磁盘占用、管理器文件合计和分项存储统计；诊断报告增加组合源文件、哈希、inode 与实际映射。
font_path=/sdcard/LuoShu/fonts/
import_path=/sdcard/LuoShu/import/
metamodule=Mountify（推荐，可选）
