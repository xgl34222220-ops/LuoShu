version=v13.5 Stable Hotfix5
changes=紧急撤回 Hotfix4 的系统字体符号链接方案，修复部分 ROM 字体生效后严重卡顿、SystemUI 无响应甚至假死；恢复普通文件/硬链接，并按 Hybrid Mount staging 展开后的真实体积限制别名数量，优先保留简体中文和 ROM 核心字体目标，达到安全预算后自动跳过低优先级英文/数字别名。
font_path=/sdcard/LuoShu/fonts/
emoji_path=/sdcard/LuoShu/emoji/
import_path=/sdcard/LuoShu/import/
hybrid_mount=请勿继续使用 Hotfix4。刷入 Hotfix5 后完整重启，并在 Hybrid Mount 中清除旧挂载错误、重建或清理旧 ext4 staging；优先使用 Magic 验证，确认稳定后再测试 Overlay。
