version=v14.3 Alpha1.10
summary=Full 模块自动安装或更新内置 App
notes=Full 包刷写时会读取 bundled/app.prop 中的包名、版本号和 SHA-256，已安装版本相同则跳过，不同则使用覆盖安装保留 App 数据与双皮肤设置；刷写环境无法调用 pm 时写入待安装标记，在首次开机完成后自动补装一次。若签名冲突或首次开机补装失败，不会每次开机重复尝试，可通过模块“操作”按钮手动重试并查看 app-install.log。Lite 包继续不内置 App，字体切换、复合生成、可变轴、覆盖诊断和双皮肤 UI 均保持不变。
