version=v2.3.8
summary=紧急预发行：回退 v2.3.7 字体激活误判，恢复组合与系统字体恢复可用性
notes=v2.3.8 紧急预发行完整回退 v2.3.7 严格启动证据链引入的真实设备回归。恢复 v2.3.6 的轻量启动状态、动态字体配置 fail-open、自挂载兼容与更新状态迁移逻辑，避免字体组合停在原子替换后失败、系统字体已经恢复却仍被旧负载回写判失败，以及启动守卫错误撤销覆盖。该版本先作为预发行供真实设备止血验证；模块 versionCode 20308，App versionCode 2030801。
