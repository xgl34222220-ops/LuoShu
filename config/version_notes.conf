version=v13.5 Stable Hotfix2
changes=完善稳定快照界面：区分当前配置、当前稳定快照与可回滚配置；显示两份快照保存时间和是否与当前配置一致；新增“立即保存当前配置”；没有可回滚配置时禁用恢复按钮并说明建立方式；保留 Hotfix1 的自救 UI 与元模块挂载兼容。
font_path=/sdcard/LuoShu/fonts/
emoji_path=/sdcard/LuoShu/emoji/
import_path=/sdcard/LuoShu/import/
hybrid_mount=支持 Magic Mount、Mountify 与 meta-overlayfs；不要手动添加 skip_mount/skip_mountify，也不要在 Hybrid Mount 中选择 Ignore。
