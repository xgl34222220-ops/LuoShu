version=v14
changes=修复字体切换期间 WebUI 偶发退出；切换状态改为轻量任务文件查询并支持重新进入后自动确认；脚本统一使用 sh 调用且开机静默校正权限，不再需要手动修复；精简自救中心，移除脚本权限和稳定配置回滚；Root 管理器说明仅显示 Android 全局字体管理与当前字体；重新精修 WebUI 并降低滚动渲染开销。
font_path=/sdcard/LuoShu/fonts/
emoji_path=/sdcard/LuoShu/emoji/
import_path=/sdcard/LuoShu/import/
metamodule=Mountify（推荐，可选）
