version=v2.0.5
summary=修复升级后字体被静默重置回默认的 reboot 死循环
notes=v2.0.5 修复三个叠加问题导致升级后每次重启字体都被重置回 default：应用字体成功后未清除"待重建"标记，开机后多重建一次；字体索引预热排在重建之后，重建一秒内误报失败；重建失败立即隔离删除用户正在使用的负载。现在应用成功即清除待重建标记，索引预热先于重建，重建失败保留当前字体并下次开机自动重试（连续 3 次失败才回退默认）。模块 versionCode 20005，正式 App versionCode 2000501。
