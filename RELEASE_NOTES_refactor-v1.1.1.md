# 洛书·重构版 1.1.1 正式版

本版重点解决实机字体扫描与切换性能，同时整理模块运行时结构，并正式启用已确认的洛书「九宫格鎏金版」App 图标。

## 字体扫描

- 安装阶段改为 fresh stock scan：从可信原厂视图重新生成本机字体清单，旧 inventory 只作为失败回退，不再污染新扫描。
- 修复 ColorOS 升级时旧 `SysFont-*` 兼容别名反复阻塞原厂重扫的问题。
- HyperOS、ColorOS 与其他 ROM 继续共用统一 v4 扫描器。
- 自动发现 `/` 与 `/system` 下额外 OEM `<partition>/fonts` 分区，并把验证通过的分区贯穿到 XML、payload、自挂载与运行时映射。
- 未知分区必须通过字体格式、coverage 与 metrics 校验，不按目录名盲目接管。

## 字体切换加速

- 对已经完成 HyperOS / ColorOS 原厂 metrics 对齐的字体 payload 建立本机专属缓存。
- 同一字体再次应用时优先复用缓存，跳过最重的 FontTools metrics 重建。
- 全局字形覆盖校验按字体文件身份缓存。
- 字体导入后低优先级后台预热；字体库打开应用确认时也提前预热。
- 前台切换会短暂等待正在完成的预热，避免前后台重复跑两套 FontTools。
- 缓存默认最多 3 份、总预算 768 MiB，防止模块目录再次异常膨胀。
- metrics 对齐流程没有删除；未命中缓存仍走完整安全路径，避免用字体偏移换速度。

## 模块结构整理

- `common/font_inventory_scan.py` 成为唯一原厂字体扫描入口，删除版本套娃的 `font_inventory_scan_v3.py`。
- Root 管理器标准入口继续留在模块根目录；内部启动核心收拢到 `.luoshu-runtime/core/`。
- 仍有迁移价值的旧兼容核心收拢到 `.luoshu-runtime/compat/v227/`。
- 删除 legacy 目录中与主运行时字节完全相同的重复 helper。
- 删除已完成使命的一次性发布清理脚本、旧版重复说明与历史残留文件。
- 模块不再把仓库整个 `config/` 目录刷进手机，只打包真正需要的运行时默认配置。
- 新增目录结构回归门禁，防止版本化入口、重复 scanner、重复文档和整包 config 再次回流。

## App

- 正式启用此前确认的洛书「九宫格鎏金版」新图标。
- 保持原包名、固定签名与数据路径，可直接覆盖安装。

## 安装

**Root 管理器中的「默认卸载模块」必须保持关闭。**

从 1.0.0 或旧系列升级可直接覆盖刷入模块并覆盖安装 App，完成后完整重启。已有字体与配置按升级迁移逻辑保留。

本版模块 versionCode 为 **60101**，App versionCode 为 **6010101**。下一正式版本为 **2.0.0**。
