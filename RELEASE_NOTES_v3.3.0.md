# 洛书 v3.3.0

洛书 3.3.0 正式版基于 v3.2.0 稳定基线，重点修复 HyperOS 应用内字体覆盖退化，并缩短字体切换与应用耗时；继续保持无 Hook、自挂载、事务回滚与加载验证架构。

## HyperOS 覆盖修复
- 以 v2.8.0 的逐 XML 目标发现能力作为 HyperOS 覆盖下限，同时保留 3.x batch 快路径。
- batch 与 v2.8 逐 XML 结果取并集；v2.8.0 能覆盖的真实字体槽不允许在新版本中退化。
- 当 batch 扫描遗漏、扫描失败或映射不完整时，由 v2.8 路径自动补齐。
- HyperOS 最终仍为 partial coverage 时直接判定失败，不再出现“模块显示成功，但部分应用仍使用系统默认字体”的假成功。
- 诊断状态增加覆盖模式、补回槽位数以及 batch / v2.8 扫描失败信息，便于真机定位。

## 字体切换性能
- 修复一次直接应用字体过程中重复执行完整 `font_config_enable_for_payload()` 的问题；同一进程内第二次调用直接复用第一次已经完成的 payload 结果。
- HyperOS v2.8 兼容目标扫描改为按 ROM XML 内容与扫描器内容建立持久缓存；系统 XML 未变化时，不再为每份 XML 反复冷启动内置 Python。
- XML 或扫描器实现变化时缓存自动失效，不以牺牲覆盖率换速度。
- 保留字重准备、设备 payload 校验、事务回滚、自挂载和加载验证，不通过缩短安全流程伪造“秒切”。

## UI 一致性
- MIUIX 与 Material 字体组合页的顶部操作按钮统一为 56dp 容器。
- 图标保持 22dp 视觉尺寸，只统一按钮外框和触控区，解决“组合工具”和“刷新”一大一小的问题。

## 继承自 v3.2.0
- 继续使用 HyperOS `--compact` 紧凑行框方案，避免标题压行、上下侵入和下伸部裁切。
- 保留九档字重、真实 ROM XML 注释兼容、复合字体 lazy 校验与完整无 Hook 覆盖链路。

## 版本信息
- 模块版本：v3.3.0
- 模块 versionCode：30300
- App versionCode：3030001

## 发布验证
正式发布工作流会重新执行完整源码检查、Android Release Lint、单元测试、固定签名 APK 构建、证书 SHA-256 校验、模块 ZIP / App 一致性检查与发布就绪门禁。

ColorOS 16、HyperOS 3、Magisk、APatch 的完整真机矩阵仍保持 pending；本次稳定版由维护者明确授权放行，不伪造未执行的真机 PASS。

<!-- release-trigger: replacement-v3.3.0-final -->
