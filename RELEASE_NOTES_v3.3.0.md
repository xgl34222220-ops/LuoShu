# 洛书 v3.3.0

洛书 3.3.0 是基于 v3.2.0 的 HyperOS 覆盖修复、字体切换性能与界面一致性更新。本版本继续保持无 Hook 全局字体方案，并按正式稳定版发布。

## HyperOS 覆盖恢复
- 将 v2.8.0 的逐 XML 动态字体目标扫描重新作为 HyperOS 的覆盖下限，同时保留 3.x batch 扫描作为快速路径。
- batch 与 v2.8 逐 XML 扫描结果会取并集，并按最终模块字体槽去重，避免新版快速扫描漏掉真实 ROM 中的 UI 字体目标。
- HyperOS 的扫描健康度以 v2.8 路径为兼容基线；batch 扫描失败可以由逐 XML 路径恢复。
- HyperOS 最终如果仍为 `partial`，现在会直接返回失败，不再把“只覆盖了一部分应用”的状态当作成功。
- 新增 `mode=hyperos-v28-union`、`batchScanFailed`、`v28ScanFailed`、`fallbackAdded` 等诊断字段，方便定位具体覆盖缺口。

## 字体切换与应用速度
- 修复一次直接字体切换中重复执行完整 `font_config_enable_for_payload()` 的问题：ROM quick 映射已经准备并安装设备负载后，后续相同负载会直接复用当前进程中的完成结果。
- HyperOS v2.8 兼容扫描增加持久缓存，缓存键同时包含 XML 内容与扫描器内容；ROM XML 未变化时，后续切换不再为每份 XML 重复启动一次内置 Python。
- XML 或扫描器内容发生变化时会自动生成新缓存键，不会为了提速复用不匹配的旧目标清单。
- 不缩短安全事务、不跳过负载校验和挂载校验，也不通过修改超时时间掩盖实际性能问题。

## 字体组合界面一致性
- 字体组合页顶部共享操作按钮统一为 56dp 容器与 22dp 图标。
- 修复 Material 风格中“组合工具”和“刷新”一个 48dp、一个 56dp 的尺寸不一致；现在与 MIUIX 风格一样保持统一视觉尺度与触控区。

## 保留的 v3.2 能力
- 保留 HyperOS `--compact` 紧凑行框修复。
- 保留九档字体准备、变量字重批处理、缓存优化和失败回退。
- 保留完整字体负载事务、失败回滚、动态目标验证、自挂载与启动后加载验证链路。

## 回归与 CI
- 扩展 HyperOS partial coverage 回归：模拟 batch 漏目标时必须由 v2.8 路径补齐；补不齐时必须失败。
- 新增字体切换速度回归：相同 XML 第二次扫描不得再次启动 Python；XML 修改后缓存必须自动失效；相同字体负载的第二次准备必须被去重；源字体变化后必须重新构建。
- 正式发布工作流会重新执行完整源码检查、Android Release Lint、单元测试、固定签名 APK 构建、证书 SHA-256 校验、模块 ZIP/App 字节一致性检查与发布就绪门禁。

## 版本信息
- 模块版本：v3.3.0
- 模块 versionCode：30300
- App versionCode：3030001

## 发布验证
ColorOS 16、HyperOS 3、Magisk、APatch 的完整真机设备矩阵仍保持 pending；本次稳定版由维护者明确授权放行，不伪造未执行的真机 PASS。

<!-- release-trigger: v3.3.0 -->
