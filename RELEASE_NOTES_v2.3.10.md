# 洛书 v2.3.10 正式版

v2.3.10 是针对 v2.3.9 字体状态误判的紧急修复版，继续保持 v2.3.8/v2.3.9 正式版 App UI，不包含实验性 UI 重构。

## 本次修复

- 修复字体已经在系统界面生效，洛书却显示“字体未生效”并弹出 `visible-font-files-mismatch` 的问题。
- 验证器优先读取真实私有负载 `.luoshu-payload`，不再把不同挂载命名空间中的公开空目录或旧兼容视图作为比较源。
- 字体文件完全一致仍属于强验证证据，但路径、inode 或系统字体服务暴露的文件布局不同不再直接等同应用失败。
- PID 1 主挂载命名空间确认可见时直接认定字体已应用。
- KernelSU、metamodule 或 OEM 字体服务造成文件布局变化时，只要挂载事务已经提交且私有字体负载存在，就记录为已应用，不再显示红色失败。
- 不再因为单纯的文件布局差异累计启动失败次数或自动恢复系统默认字体。
- 只有自挂载明确失败且没有更强的系统可见证据时，才判定为真正失败。
- 增加私有负载、可见文件完全匹配、挂载后布局变化和明确挂载失败的回归测试。

## 版本信息

- 模块版本：`v2.3.10`
- 模块 versionCode：`20310`
- App versionName：`2.3.10`
- App versionCode：`2031001`
- 发布类型：正式版
