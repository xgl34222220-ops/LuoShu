# LuoShu video-pixel UI rebuild

This branch treats the supplied 93-second reference recording as the only visual source of truth.

Current pass:

- centered compact home title with no side controls that can clip text;
- reference-style status card and equal-width control strip;
- compact grouped device-status rows;
- four-item dock: Home, Library, Studio, Settings;
- guaranteed content clearance above the floating dock;
- task center presented as a right-side rounded panel.

The branch remains review-only until real-device screenshots match the reference recording.
