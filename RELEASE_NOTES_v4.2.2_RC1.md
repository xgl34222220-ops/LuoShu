<!-- prerelease -->
# 洛书 v4.2.2 RC1

修复同一字体在 HyperOS 上生成 GB 级副本，却未覆盖部分 Chrome 字体路径的问题，同时修正 ColorOS 原厂字体重扫的误报。本版递增版本号，允许从 4.2.1 RC1 正常升级；稳定渠道仍为 4.2.0。

## 修复

- HyperOS 不再用 `NotoSans*` 泛匹配界面槽位。无关语言回退字体保留原厂文件，旧扫描清单也不能重新生成这些错误副本。对真实 K80 原厂 `/system/fonts` 的 230 个文件名重放，匹配数由 128 降至 14；这是按字体用途筛选的结果，没有硬性限制为 14 个槽，其他分区仍可增加必要目标。
- HyperOS 固件存在经 `MiSansVF_Overlay.ttf`、`theme_webview` 转向主题 `Roboto-Regular.ttf` 的独立路径。现在仅在确认这条路径实际指向主题字体时，为 Chrome/Google 与 Zygote 的可访问挂载空间提供当前字体副本，并采用主题目标的排版参数。保留系统动态链接与原主题文件，主题路由改变、停用或恢复默认时撤销本模块挂载。
- ColorOS 重扫会从可信原厂目录补读仍存在的旧槽位，避免仅因 XML 或发现规则漏列就拒绝整份更新；真正缺失或不可信的文件仍保留旧清单。ColorOS 原生主字体优先于闲置 MiSans，避免误选排版参数。
- 构建策略变更时，升级保留当前已应用字体，但不继承旧的设备生成缓存，并明确提示重新应用一次。仅重启不能误清除待重建标记；成功应用后才清除。用户字体库保留。
- 主题路径维护复用现有单实例字体服务，以轻量指纹检测变化；空闲时不反复启动 Python，主题副本缓存有数量上限。保留 4.2.1 RC1 的状态查询与连接超时相关修复。

## 安装

1. 刷入本页模块 ZIP，完整重启。
2. 在洛书中重新应用当前字体或字体组合，等待成功后再次完整重启，让旧大副本按新规则重建。
3. 重新打开 Chrome 与此前显示异常的应用。模块内置与独立 APK 相同的固定签名 App。

无需手动删除用户字体或主题文件。仅升级而不重新应用，仍可能继续使用保留下来的旧字体负载。

## 验证范围

在构建机使用真实 20 MB MiSans VF 字体、K80 原厂文件名及模拟的不同槽位排版参数进行受控构建，物理字体文件合计由约 2.415 GB 降至约 85.6 MB。这证实旧匹配规则能造成 GB 级膨胀，但不是 K80 真机释放空间的测量，不包含模块运行时、所有缓存或其他保留负载。模块管理器显示的模块大小不能直接当作运行内存。

新增回归验证覆盖旧槽位迁移、错误语言槽排除、升级标记、主题路径恢复、完整字形保留、缓存上限与服务空闲行为。K80 / 一加真机显示回归仍待确认，不能据此宣称 QQ、酷安、遥控器偏移或所有连接超时已经消失。

Chrome 同时存在系统字体、Google 下载字体和页面自带字体等来源。此修复针对已识别的系统主题路径与现有 Google 字体桥接；已被进程缓存的字体需应用重新启动才可能更新，不保证覆盖网页指定的所有字体。

参考一手实现：[Skia Android 字体配置读取](https://skia.googlesource.com/skia/+/refs/heads/main/src/ports/SkFontMgr_android_parser.cpp)、[Chromium Google 字体下载与文件描述符缓存](https://chromium.googlesource.com/chromium/src/+/refs/heads/main/content/public/android/java/src/org/chromium/content/browser/font/AndroidFontLookupImpl.java)。
