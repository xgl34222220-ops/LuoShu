# 洛书·重构版 1.0.0 正式版

按维护者要求，洛书的公开版本历史从本重构版重新开始：**重构版 1.0.0 是新的第一个版本**。旧系列 Release、预发行版和版本标签已全部清理，源码提交历史保留。重构版不是回退到旧代码，继续包含现有 HyperOS 字体覆盖、压缩复用、Google 字体维护机制以及中文 Google 字体兼容。

## 中文 Google 字体兼容

入口：**设置 → Google 字体兼容**。中文状态、重新检测、开启、恢复、二次确认及使用说明已内置，不需要终端。

- 默认不开启。安装、升级、开机、进入页面和「重新检测」均不自动停用组件。
- 「开启 Google 字体兼容」只停用当前 App 所属 Android 用户的 GMS 字体提供组件，不停用整个 GMS，不清除账户、App 数据或字体缓存。
- 影响该用户所有依赖 GMS 下载字体的应用，可能包括下载式表情字体；修改组件时相关 GMS 进程可能重启。它不是只作用于商店的开关。
- 「恢复原设置」按记录还原原始组件状态，不接管其他工具的停用设置。
- 之前独立脚本的恢复记录继续读取，核验通过后显示已开启，无需反复开启。

## 安装与使用

**「默认卸载模块」必须保持关闭。** 覆盖刷入模块 ZIP，并覆盖安装配套 APK，随后完整重启。

从此前内部/历史版本升级的用户无需卸载 App、清除数据或重新导入字体。进入兼容页点击「重新检测」，显示已开启就不必重复操作。首次使用者应先应用自定义字体，再开启兼容并完整重启，检查商店英数及后台重开后的效果。

**停用或卸载洛书前，先点击「恢复原设置」并完整重启。** 组件设置跨重启保存，单纯停用模块不能保证恢复。卸载脚本会尽力恢复属于洛书记录的设置，但不替代手动恢复。

## 新编号与升级兼容

正式版本从这里重新计数：**1.0.0 → 1.1.1 → 2.0.0 → 2.2.2 → 3.0.0 → 3.3.3 → 4.0.0 → 4.4.4……**

本版 App 显示 1.0.0；模块内部 versionCode 为 60000，App 为 6000001，用于保证对旧安装可正常覆盖升级。保持原 App 包名、模块 ID 和固定签名。

本版唯一公开版本标签为 `refactor-v1.0.0`。旧系列 Release、预发行版及版本标签已经删除，因此 GitHub 的公开版本列表从本版开始。源码提交历史没有删除。

正式与预览更新通道均指向本版。

## 验证说明

沿用已签名集成版本的 Google 兼容实现，本次版本历史重置没有改变字体引擎和组件控制行为。后端事务回归、编号与升级元数据测试、完整源码检查、Android lint / 单元测试、固定证书校验及模块完整性检查均按发布流程执行。

K80 至尊版 / 一加 15 的长期字体保持、覆盖、功耗与重启稳定性项目仍按实际证据保留待测；模拟器 UI 检查曾受 KVM 不可用阻断。组件「已开启」只说明状态核验成功，不等于所有 Google 页面永不回退，App 内置或网页指定字体不保证被替换。
