# 洛书·重构版 1.0.0 正式版

按维护者本次要求，将已交付的中文 Google 字体兼容集成发布为重构版 1.0.0。重构系列从 1.0.0 重新编号，不是回退到旧版源码；保留现有 HyperOS 字体覆盖、压缩复用与无 Hook 守护。

## 中文 Google 字体兼容

入口：**设置 → Google 字体兼容**。中文状态、重新检测、开启、恢复、二次确认及使用说明已内置，不需要终端。

- 默认不开启。安装、升级、开机、进入页面和「重新检测」均不自动停用组件。
- 「开启 Google 字体兼容」只停用当前 App 所属 Android 用户的 GMS 字体提供组件，不停用整个 GMS，不清除账户、App 数据或字体缓存。
- 影响该用户所有依赖 GMS 下载字体的应用，可能包括下载式表情字体；修改组件时相关 GMS 进程可能重启。它不是只作用于商店的开关。
- 「恢复原设置」按记录还原原始组件状态，不接管其他工具的停用设置。
- 之前独立脚本的恢复记录继续读取，核验通过后显示已开启，无需反复开启。

## 安装与使用

**「默认卸载模块」必须保持关闭。** 保留可用安装包，覆盖刷入模块 ZIP，并覆盖安装配套 APK，完整重启。

已有 4.4.4、5.0.0-Beta1 或独立脚本设置的用户，无需卸载 App、清除数据或重新导入字体。进兼容页点击「重新检测」，显示已开启就不必重复操作。首次使用者应先应用自定义字体，再开启兼容并完整重启，检查商店的英数及后台重开后的效果。

**停用或卸载洛书前，先点击「恢复原设置」并完整重启。** 组件设置跨重启保存，单纯停用模块不能保证恢复。卸载脚本会尽力恢复属于洛书记录的设置，但不替代手动恢复。

## 新编号与升级兼容

后续正式版固定顺序：**1.0.0 → 1.1.1 → 2.0.0 → 2.2.2 → 3.0.0 → 3.3.3**，不再延续旧系列的 5.0.0。

本版 App 显示 1.0.0；模块内部版本代码为 60000，App 为 6000001，均高于已交付的旧系列 4.4.4 和 5.0.0-Beta1。保持原 App 包名、模块 ID 与固定签名，可覆盖升级。新系列标签使用 `refactor-v1.0.0`，与旧系列历史标签隔离；不覆盖旧 4.0.0 或其他历史版本。

正式与预览更新通道在安装包发布并校验后同步到本版。此次没有新的旧版本删除操作。

## 验证说明

沿用已签名集成版本的 Google 兼容实现，本次只调整发布系列、编号、升级校验和说明。执行后端事务回归、编号与升级元数据测试、完整源码检查、Android lint / 单元测试、固定证书校验及模块完整性检查。

维护者明确授权本次正式发布。K80 至尊版 / 一加 15 的长期字体保持、覆盖、功耗与重启稳定性项目仍按实际证据保留待测；此前模拟器 UI 检查受 KVM 不可用阻断，不写成已通过。组件「已开启」只说明状态核验成功，不等于所有 Google 页面永不回退，App 内置或网页指定字体不保证被替换。
