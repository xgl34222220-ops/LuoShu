# 洛书 v3.3.5

这是 v3.3.4 运行时恢复版的界面尺寸修正版，不改动字体生成、覆盖映射或开机挂载逻辑。

## 顶栏修正

- 字体组合页右上角刷新按钮不再使用突兀的大方块。
- 工具与刷新按钮统一为 36 dp 可见容器、18 dp 图标和 12 dp 圆角。
- 视觉按钮缩小的同时保留 48 dp 触控区，避免影响点击。
- 两个按钮显式使用同一主题色和低阴影，刷新图标不再出现纯黑或视觉重量不一致。
- Miuix 与 Material 字体组合页使用同一按钮组件，标题统一收紧为 30 sp。

## 运行时边界

- 字体运行时保持 v3.3.4 恢复的 v3.0.0 基线不变。
- 从 v3.3.4 升级不会清除已经生成的干净字体负载。
- 直接从 v3.1–v3.3 升级仍会清除旧问题负载，首次开机使用系统字体，重新应用后生成干净负载。

## 版本

- 模块：v3.3.5（30305）
- App：v3.3.5（3030501）
