# 洛书 v2.5.5

本次正式版针对 v2.5.4 字体详情页的真机上下抽动问题进行稳定性修复，不改变字体引擎、provider bridge、挂载、事务回滚、字体应用或字体组合算法。

## 字体详情稳定性
- 关闭 `ModalBottomSheet` 自身纵向拖拽，避免与详情内部 `verticalScroll` 竞争纵向手势。
- 详情可视区域固定为屏幕可用高度的 94%，减少内容变化触发 Sheet anchor 反复重算造成的上下跳动。
- 每次打开或切换字体详情时将内部滚动位置复位到顶部，避免沿用上一次详情的滚动偏移。
- 不可拖拽状态下移除顶部拖拽条，避免交互语义与实际行为不一致。

## 影响范围
- 仅调整字体详情弹层的布局与滚动交互。
- 字体预览、综合 / 中文 / English / 数字四类预览模式保持不变。
- 字体引擎、provider bridge、系统/OEM 挂载、事务回滚、字体应用和组合算法均未改动。

## 验证
发布链继续执行：
- `scripts/check.sh`
- Android Release Lint
- Android 单元测试
- 正式签名 APK 构建与签名证书校验
- 模块 ZIP 构建、内嵌 APK 一致性与 SHA-256 校验
- 正式发布就绪门禁

## 真机状态
本版本直接针对用户提供的真机录屏现象修复；ColorOS 16、HyperOS 3、Magisk、APatch 的完整设备矩阵仍保持 pending，本次稳定版由维护者明确授权放行，不伪造未执行的真机 PASS。
