# 洛书 v3.3.7 候选版

本版集中修复从 v3.1–v3.3 延续下来的三类关键问题：直接切换耗时过长、首次重启恢复默认或需要重复应用，以及 HyperOS 只替换中文而英文数字仍为系统字体。同时按参考软件重做 Miuix 底栏与页面转场。

## 字体引擎

- 一次点击只提交一套最终负载；直接切换不再读取、生成或安排设备对齐缓存，成功提示之后不会再有后台任务覆盖当前结果。
- 启动事务写入 `bootId`，同一开机重复调用保持幂等；只有上一轮开机未确认时才安全撤销字体负载。
- 负载清单按设备号和 inode 复用校验值。MiSans、Roboto、数字字重等硬链接到同一源时只读取源字体一次。
- HyperOS 完整覆盖 MiSans、MiSansLatin、Roboto、GoogleSans、100–900、Mitype、MiClock、AndroidClock 与 Clockopia 等真实 UI 槽位。
- 字体具备可用拉丁字母和完整数字时即可覆盖英文数字槽；缺少的个别标点继续由系统 fallback 补齐，不再导致整套英文映射被跳过。
- 字形能力缓存升级到 v4；旧版把完整英数字体误记为不可用的缓存会自动失效并重新检测。
- 显式切换会取消旧版本遗留的待处理缓存任务；已经运行的旧 worker 在激活前也会重新检查取消标记，不能覆盖新提交。

## 界面与动效

- Miuix 悬浮底栏采用背景取样模糊、半透明高光玻璃壳、浅蓝液态选中层三层结构。
- 选中层随栏目弹性滑动并短暂拉伸，底栏显示与隐藏带有位移和淡入淡出。
- 顶层栏目改为大幅横向联动转场；任务页和设置详情从右侧以圆角卡片覆盖进入。
- 页面背景增加极轻的主题色光晕，同时保留紧凑卡片、列表密度和小尺寸顶栏操作。
- 刷新按钮可视容器保持 30 dp，触控区保持 48 dp，不再出现明显大于相邻按钮的方块。

## 版本

- 模块：v3.3.7（30307）
- App：v3.3.7（3030701）

该构建先作为真机候选验证：重点确认一次应用、一次完整重启即可生效，中文/英文/数字覆盖一致，并观察是否存在启动循环。通过后再更新稳定版授权与在线更新元数据。
