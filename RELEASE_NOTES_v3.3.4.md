# 洛书 v3.3.4

这是一次运行时恢复版本，不再在 v3.3.x 的问题链路上继续叠加补丁。

## 恢复内容

- 字体实例化、九档字重、XML overlay、字体度量、HyperOS 物理槽、自挂载和启动回滚全部恢复为 v3.0.0 的实现。
- 移除 v3.1.0 引入的变量字体实例缓存。
- 移除 v3.2.0 引入的九档批处理、XML 扩展和 HyperOS `--compact` 行框改造。
- 移除 v3.3.0 引入的 HyperOS v2.8 覆盖下限层、目标扫描缓存和 payload 去重热修复。
- 保留 v3.3.3 的紧凑 App 界面以及真实挂载失败目标显示。

## 安全升级

从任何旧版首次升级到 v3.3.4 时，安装器不会继承旧版生成的活动字体 payload：

- 字体文件、组合选择和字重偏好仍会保留；
- 旧的活动挂载、XML、任务状态和启动状态会被清除；
- 第一次开机使用系统默认字体；
- 进入洛书重新应用字体后，负载由恢复后的 v3.0.0 引擎重新生成。

这样可以避免 3.1–3.3 的旧负载继续造成覆盖不完整、重启后不生效或循环重启。

## 版本

- 模块：v3.3.4（30304）
- App：v3.3.4（3030401）
