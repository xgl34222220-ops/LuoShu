<!-- prerelease -->
# 洛书 v4.2.1 RC1

这是针对 v4.2.0 字体副本膨胀、连接查询超时和 Google 字体维护遗漏的修复测试版。

## 修复

- HyperOS 英数槽此前只移除了中文字符映射，仍携带整套中文字形。现在仅在已确认中文回退覆盖时真正精简字形；同一来源与路由共用一次处理，保留每个槽独立的排版参数、标点和变体字符。扫描槽位数不直接等于独立字体副本数，不按固定 14 个槽截断必要覆盖。
- Google 下载字体副本只保留需要的非 CJK 字符，避免为每个字重重复保存、实例化完整中文字体。旧副本通过新版缓存键逐步替换。
- Google 字体维护补充 Chrome 稳定版、测试版及其他 Google 应用进程，修复应用重新启动后遗漏挂载命名空间的问题。
- App 状态查询不再重建组合运行目录、调用 Root 管理器版本进程或同步执行深度字体验证。粗细查询不再触发旧字体库迁移；缺省配置时系统设置只读取一次。未核实的状态仍保留待重启提示。

## 安装与验证范围

刷入本页模块 ZIP，完整重启；在洛书中重新应用当前字体或字体组合，完成后再次完整重启，使现有大副本按新逻辑重建。模块内置与独立 APK 相同的固定签名 App。请保留用户字体文件，不需要手动清空字体库。

本机真实 MiSans VF 样本的英数来源由 20,093,424 字节缩至 575,912 字节，95 个 ASCII 字符在最细、默认、最粗字重下轮廓及宽度一致。这是构建机样本结果，不是 K80 的实际内存或释放空间测量。模块管理器的体积数字不能直接当作进程内存占用。

K80 / 一加真机回归仍待测，尚不能宣称所有应用偏移或所有连接超时均已消失。Google 修复覆盖已识别的下载字体与可访问的进程挂载空间；应用内置字体、网页自带字体及已经加载的 Typeface 不保证即时改变，已打开的应用可能需要保存内容后重新启动。本测试版不标记为最新稳定版。
