<!-- prerelease -->
# 洛书 v2.4.1 Beta 1 预发行版

这是 v2.4.0 之后的稳定性验证版本，重点加固字体切换事务锁，不改变现有 App UI、字体覆盖策略和 Play/GMS 下载字体链路。

## 主要更新

- 字体切换锁从单独 PID 校验升级为 `PID + process starttime + boot_id` 身份校验。
- 避免旧任务退出后 PID 被 Android/Linux 重新分配时，把无关新进程误判成仍在执行的字体切换任务。
- 手机重启后可识别旧 boot_id 残留锁，避免历史锁阻塞新一次字体切换。
- 继续兼容旧版仅记录 PID 的 `.font_switch.lock`，升级后无需手动清理锁文件。
- 加强锁初始化阶段的竞争保护，并在释放锁时校验锁所有者身份，避免错误进程释放他人的锁。
- 扩充字体切换锁回归测试，覆盖并发抢锁、异常退出、死 PID、PID 身份不匹配、boot_id 变化和旧锁兼容。
- 发布负载清单同步包含新的通用工具核心文件，确保模块 ZIP 与源码校验一致。

## 测试重点

建议重点测试连续快速切换字体、切换过程中退出 App、切换任务异常中断、重启后再次切换，以及 Magisk / KernelSU / APatch 下的升级与恢复流程。

## 安装说明

这是预发行版本。可直接覆盖安装并完整重启；稳定版用户如果不需要验证上述并发与残锁场景，可以继续使用 v2.4.0。
