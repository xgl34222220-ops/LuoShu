# 洛书 v2.5.0 Beta 1 实现批次

本批次建立在 `feat/safety-update-presets` / `12de966e777c87f972758fe23c27f49b0223b2ca` 之上。

## 已实现

- 修复 `SystemCenterViewModel.updateChannel` JVM setter 与手写 `setUpdateChannel()` 的签名冲突。
- 完整备份/恢复：用户字体、收藏/标签、本地方案库、当前 Studio 方案、外观设置和可迁移配置。
- 备份逐文件 SHA-256 校验、ZIP 路径穿越保护、文件数/体积上限。
- 成功切换历史：只在直接字体任务或组合任务真正成功后记录；保留最近记录；通过现有切换/组合控制器恢复。
- A/B 场景预览：混排、正文、微信文章、Play 商店、状态栏数字、金额、小字号、粗体标题。
- 缺字地图：简体、繁体、日文假名、韩文、Latin、Latin Extended、数字、标点、数学符号、PUA，并保留旧 coverage JSON 字段兼容。
- 新增 v2.5 Beta 1 专项 GitHub Actions 工作流。

## 安全约束

- 不重写字体映射核心。
- 不将运行时 payload、锁、任务、缓存、日志、当前挂载状态带入跨设备备份。
- 回退不直接复制系统文件，继续走 `font_switch_task.sh` / `font_mix_controller.sh` 的校验与事务路径。
- 缺字诊断保持只读。

## 本地已通过

- `python3 -m py_compile scripts/apply_v25_beta1.py common/font_coverage_info.py`
- `scripts/switch_history_test.sh`
- `scripts/full_backup_root_test.sh`
- 增强 coverage 实际字体输出断言
- GitHub Actions YAML 解析
- 新增 Kotlin 文件语法探针

## 仍需远端 CI 验证

完整 Android Gradle `testDebugUnitTest lintDebug assembleDebug` 和仓库原有全部回归测试，需要把补丁应用到 GitHub 功能分支后由 Actions 执行。本批次未宣称这些远端检查已经通过。
