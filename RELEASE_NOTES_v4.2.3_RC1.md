<!-- prerelease -->
# 洛书 v4.2.3 RC1

用于复测同一组字体在旧版可用、升级后组合失败的问题。模块版本代码 40203，App 版本代码 4020301，使用原有发布签名，可覆盖升级。

## 本版修正

- 组合检查保留真实失败原因，区分读取或执行失败与实际缺字；覆盖检测不再丢掉后台返回的错误。
- 字重分析返回真实退出码；读取失败或缺失轴数据时，不再显示为“固定 400”。
- 失败结果不缓存，提供重试；同名字体更新后，使用文件指纹重新读取字重。
- 保留 4.2.2 RC1 的 HyperOS 槽位筛选、主题字体路径与 ColorOS 重扫修复。

## 安装与测试

1. 在 Root 管理器中刷入模块 ZIP，完整重启。ZIP 已内置同签名 App；也可用本页 APK 覆盖更新 App。
2. 打开洛书，用原来的“花轮丸 + 鸿蒙保时捷可变VF”组合生成并应用。可变字体读取失败时可直接点“重试”。
3. 生成成功后按 App 提示重启，检查组合字体、Chrome 英文和数字。

**KernelSU 的“默认卸载模块”需关闭。** 保留现有字体库，无需删除或重新导入原字体。

## 验证范围

App 全部 117 项单测、16 项角色检查及桥接测试已通过，编译通过。本版修正了已复现的误报和缓存问题；用户设备首次读取失败的原因尚未复现，组合生成、K80 偏移及 Google 字体保持情况仍需实测。此版本为测试版，稳定渠道继续保留 4.2.0。
