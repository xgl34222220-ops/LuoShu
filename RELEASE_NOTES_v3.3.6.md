# 洛书 v3.3.6

这是字体切换性能修正版。它恢复旧版“先快速映射、再后台增强”的前台边界，同时保留当前版本的安全事务与完整覆盖能力。

## 切换速度

- 直接应用字体不再在前台遍历多分区字体 XML。
- 缓存未命中时不再同步准备九档字重或启动 XML/Python 生成器。
- 前台只完成真实字体槽位硬链接映射、安全快照、负载校验和原子提交。
- 已有可信设备对齐缓存仍会立即激活；未命中时只登记低优先级后台增强任务，不阻塞 App。

## 重复切换

- 字形覆盖校验改为按字体文件身份分别缓存，不再只有一条全局缓存。
- 缓存同时保存中文与拉丁字形能力，恢复缓存后仍按正确槽位策略映射，不降低缺字保护。
- 字体文件被替换、修改或重新导入后，身份变化会自动触发完整重验。

## 安全边界

- 保留切换锁、安全快照、失败回滚、负载校验、原子提交和重启门禁。
- 完整设备对齐仍由低优先级后台任务生成；连续失败预算和开机保护保持不变。
- 保留 v3.3.5 的 36 dp 顶栏按钮与 18 dp 图标修正。

## 版本

- 模块：v3.3.6（30306）
- App：v3.3.6（3030601）
