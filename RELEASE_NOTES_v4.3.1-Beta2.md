<!-- prerelease -->
# 洛书 v4.3.1-Beta2

Google 英文、数字反复恢复默认的无 Hook 守护修复测试版，基于 Beta1。正式更新通道仍为 v4.3.0。

## 本次修改

- 恢复系统默认字体或尚未选字体时，守护暂停并只撤销一次本模块覆盖；同一次开机内重新启用字体后自动继续，不再永久退出。
- Google 下载字体目录发生创建、写入完成、替换、删除时唤醒检查，不再只依赖 30 秒盲等。只观察已知字体目录，不读取或修改其他应用数据。
- 每 2 秒检查 Google/Chrome 进程身份变化；修复新进程与已有进程共享挂载环境时被漏掉，以及 PID 复用未被识别的问题。字体挂载仍按环境去重，不因进程增多反复复制大字体。
- 保留 30 秒完整元数据复查。事件观察器不可用时退回原定时检查，不忙循环。提前唤醒不错误消耗故障重试间隔。
- 停止守护可取消等待并释放锁；保留既有只刷新已确认持有旧字体的后台进程策略，不强停前台应用，不停用 GMS，不封禁联网。
- 保留 Beta1 的 HyperOS 英数入口恢复、中文回退、字体子集和载荷复用。未加入 Hook。

## 安装

**默认卸载模块一定要关闭。** 保留可用版本。覆盖安装模块和配套 App 后完整重启。已在 Beta1 重新应用过字体的，本次不要求再生成字体；从 4.3.0 升级且未重建过的，仍需重启后重新应用当前字体并按提示重启。

重点测试：Google Play/Chrome 原来反弹的英数；放后台再打开；切回默认字体再启用同一字体；连续重启。出现异常可停用模块并重启，回到保留版本，不要删除活动字体目录。

## 验证边界

主机测试覆盖真实 Linux inotify 事件、缓存原子替换、目录重建、共享挂载环境中新进程、PID 复用、守护暂停恢复及取消。原 Beta1 的进程指纹在两个新回归用例上失败，修改后通过。

事件通知不是加载前权限拦截，仍可能晚于应用第一次打开字体；已经缓存的旧文件仍需现有刷新流程处理。本版不修改 Google 字体提供方代码或强行替换进程内存，不保证覆盖 App 内置或网页指定字体。标准库事件等待子进程不加载 FontTools，但其 Android 常驻/峰值开销尚需真机测量。没有连接 K80 至尊版或一加 15，不把构建检查当作真机覆盖、功耗或稳定性验收。
