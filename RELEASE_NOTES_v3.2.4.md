# 洛书 v3.2.4

洛书 3.2.4 是针对 v3.2.3 在一加 15 / ColorOS 16 上出现的启动黑屏循环重启，以及字体切换明显变慢问题的紧急稳定版修复。

## 启动安全修复

- OPlus 系设备（OnePlus / OPPO / realme）不再对核心 `/system/fonts` 叠加整目录 OverlayFS。
- OPlus 核心字体树改走逐文件 bind 兼容路径，只替换 ROM 本机真实存在的字体文件槽，不创建不存在的附加字体目标。
- 当本次洛书系统挂载探针已经可见时，直接复用现有 `/system/fonts` 挂载，禁止再次叠加第二层自挂载。
- 原有原子事务、可见性验证和失败完整回滚继续保留；本修复没有把部分挂载或失败状态降级伪装成成功。

## 字体切换性能

- 以前每次切换都会重新生成 100–900 九个 UI 字重和九个等宽字重，即使输入完全没变，也会重复启动嵌入式 Python / FontTools。
- v3.2.4 为这 18 个生成文件增加输入身份缓存键：同时记录所有源字重、字体处理引擎和归一化版本。
- 输入与引擎完全一致时直接复用已生成负载，跳过重复字体转换。
- 任意源字重或处理引擎发生变化时缓存自动失效并重新生成，不牺牲正确性换速度。

## 回归验证

- 新增生成字重缓存命中与失效回归：未变化输入不得再次进入生成阶段，源字重变化必须强制重建。
- No-Hook 字体引擎、字体 smoke、发布就绪门禁继续覆盖原子挂载、字体负载与 Android 构建链。
- ColorOS 16 / 一加 15 的本次问题来自真实设备反馈；完整 ColorOS、HyperOS、Magisk、APatch 真机矩阵仍如实保持 pending，不伪造未执行的 PASS。

## 版本信息

- 模块版本：v3.2.4
- 模块 versionCode：30204
- App versionCode：3020401

<!-- release-trigger: v3.2.4 -->
