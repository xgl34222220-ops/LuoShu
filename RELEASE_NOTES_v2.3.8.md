<!-- prerelease -->
# 洛书 v2.3.8 紧急修复版

v2.3.8 用于修复 v2.3.7 在真实设备上无法切换字体、字体组合失败以及恢复系统字体异常的问题。

## 本次修复

- 回退 v2.3.7 过度严格的本次启动字体证据链，恢复 v2.3.6 的兼容状态判断。
- 删除原子自挂载对 PID 1 `mountinfo` 目标必须存在的强制门禁。
- 删除 bind 源文件与系统可见目标必须具有相同设备号和 inode 的强制门禁。
- 字体挂载成功且目标内容、大小和快速指纹一致时即可提交事务，不再因 ROM、KernelSU、SukiSU 或 APatch 的命名空间差异误回滚。
- 动态字体配置路径继续采用 fail-open：设备不支持或 ROM 更新配置时保留系统原配置，不再直接导致整个字体组合失败。
- 恢复系统字体时不再被过期启动状态和旧字体负载证据误判失败。
- 保留完整原子安全策略：真实字体或必要配置挂载失败时仍会全部逆序回滚，不允许系统出现半套字体。

## 重点验证

安装后请依次验证：

1. 切换一个普通单字体，任务能够完成并提示重启。
2. 使用字体组合功能，能够越过“正在准备原子替换字体负载”并完成。
3. 完整重启后确认系统字体实际变化。
4. 恢复系统字体并重启，确认原厂字体恢复且任务不再报旧负载回写失败。

## 版本信息

- 模块版本：`v2.3.8`
- 模块 versionCode：`20308`
- App versionName：`2.3.8`
- App versionCode：`2030801`
- 发布类型：紧急预发行版
