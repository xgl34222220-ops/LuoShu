# 洛书 v3.3.8 真机验收候选

这版不沿用 v3.3.7 的“文件槽先成功、XML 后处理”路径，而是把字体文件、英文数字家族映射和启动挂载收进同一个原子负载。目标是一次应用、一次完整重启即可生效，同时避免分钟级等待和重启循环。

## 字体切换

- 物理字体槽、`LuoShu-100` 至 `LuoShu-900`、等宽别名和动态字体 XML 全部写入 `.luoshu-payload`，不再出现 App 写入目录与开机挂载目录不一致。
- 第一次为设备生成轻量 XML；之后切换字体只重指向硬链接。前台路径不运行 Python、不制作九份大字体、不查询或安排设备缓存 worker。
- 一次点击只提交一个最终负载；成功后没有后台任务再次替换它。
- 恢复系统字体会同时移除字体文件与 XML；字体到字体的切换则复用已验证、与具体字体内容无关的 XML 模板。

## HyperOS 中英数字覆盖

- 按设备实际存在的字体文件映射 MiSans、MiSansLatin、Roboto、GoogleSans、Mitype、100–900 数字字重、MiClock、AndroidClock、Clockopia 等文本槽。
- 新增 `/mi_ext/fonts` 分区发现与同步。
- 组合字体也使用完整槽位清单，不再只替换中文基底而遗漏外部 App 直接请求的英文或数字族。
- TTC 等不适合无损创建 `.ttf` XML 别名的输入会安全保留物理槽映射，不以错误 XML 冒险启动。

## 界面与动效

- 底栏按参考视频改成 52 dp 的单层透明取样玻璃底板，选中项是独立的浅蓝弹性透镜；栏目切换继续保留滑动、拉伸和整页横向动效。
- 模糊只作用于底板，不包裹图标、文字和透镜，修复部分 HyperOS GPU 合成器产生的白色矩形块。
- 顶栏刷新与工具按钮为 40 dp 可视容器、21 dp 图标和 48 dp 触控区。
- 更新页下载按钮为 64 dp，标题与 SHA-256 独立排版，不再截掉说明。
- 首页状态提示去掉额外阴影层并使用整行普通卡片，避免文字后出现第二块不透明背景。

## 版本与验收

- 模块：v3.3.8（30308）
- App：v3.3.8（3030801）

本构建先用于真机验收。请重点验证：应用一次后只需一次完整重启；123 云盘等外部 App 的中文、`REDMI K80`、英文和数字使用同一套选中字体；连续切换两款字体不等待数分钟；底栏没有白块且滑动透镜连续。通过这些项目后再更新正式稳定渠道。
