# 洛书 v2.6.0

本次正式版完成 MIUIX 悬浮底栏的 Liquid Glass 视觉升级，并修复真机出现的选中项白色矩形渲染伪影。字体引擎、provider bridge、挂载、事务回滚、字体应用和组合算法均保持不变。

## Liquid Glass 底栏
- MIUIX 悬浮底栏从偏白磨砂改为更透明的玻璃采样层，降低白色覆盖、Blur 半径与 Noise，保留后方卡片、颜色和页面结构。
- 页面内容允许延伸到底栏后方，让玻璃真正采样到内容，而不是只模糊纯色背景。
- 加入镜面高光、Fresnel 风格边缘光和主题色 caustic，增强玻璃的立体与环境反射感。
- 选中态改为独立移动的液态透镜，并使用更有弹性的 spring 切换。

## 真机白块修复
- 针对部分 OEM/GPU 合成路径下选中 Tab 出现白色矩形的问题，移除选中项 `graphicsLayer` 缩放。
- 移除液态透镜额外 `shadow()` 离屏合成路径，避免 Haze 与离屏层叠加触发白底伪影。
- 按压反馈改为不创建额外离屏图层的实现，保留交互反馈同时提高兼容性。
- UI 回归检查新增约束，防止高不透明白色底栏、旧 Blur 参数和 OEM 敏感合成路径回流。

## 兼容与影响范围
- Material 模式底栏保持原行为。
- 字体详情页 v2.5.5 的稳定滚动修复继续保留。
- 字体引擎、provider bridge、系统/OEM 挂载、事务回滚、字体应用和字体组合算法未改动。

## 验证
正式发布链继续执行：
- `scripts/check.sh`
- Android Release Lint
- Android 单元测试
- 正式签名 APK 构建与签名证书校验
- 模块 ZIP 构建、内嵌 APK 一致性与 SHA-256 校验
- 正式发布就绪门禁

## 真机状态
本版本已根据真机截图修复底栏白色矩形渲染伪影；ColorOS 16、HyperOS 3、Magisk、APatch 的完整设备矩阵仍保持 pending，本次稳定版由维护者明确授权放行，不伪造未执行的真机 PASS。
