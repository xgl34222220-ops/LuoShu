# 洛书 v4.3.0

以 v4.2.0 为基础整理的正式版，重点处理 HyperOS 字体占用和 Google 应用字体恢复问题，沿用 4.2.0 的 App 界面与字体组合流程。

- 精简 HyperOS 物理字体目标，保留实际 UI、中文及数字字体入口，避免把无关语言字体全部复制成大中文字体。
- 英文槽使用真正的字体子集，相同字体和度量复用生成结果，减少文件占用及生成时内存峰值。
- 更新时跳过已失效的生成缓存，保留原始字体与当前可用载荷；重新应用会使用新策略重建。
- 持续维护 Google 下载字体和 HyperOS 主题字体路由，补齐 Chrome/Google 独立挂载环境，处理缓存文件和路径重建。
- 对仍缓存旧字体文件的 Chrome/Google 后台进程进行有证据的刷新；不强停前台应用，切到后台后由系统回收再重新取字。
- 降低 App 状态、粗细查询和后台空闲维护开销，避免查询触发字体迁移或重新生成；已退出的组合任务自动释放。

## 升级

覆盖安装模块与随包 App，重启后重新应用一次当前字体或组合，再完整重启，使旧大体积载荷替换为精简载荷。无需重新导入已有字体。不要手动删除正在使用的字体目录。

## 验证范围

受控 Linux 构建测试使用约 20 MB MiSansVF、K80 字体文件名清单和模拟原厂度量：生成字体唯一 inode 字节从 2,551,899,417 降至 85,569,363，构建进程峰值 RSS 从 256,704 KiB 降至 145,084 KiB。此数据不代表手机常驻内存或整个模块大小；实际结果取决于所选字体及 ROM。

签名构建、源码回归、升级保留与字体路由检查通过后发布。K80 至尊版和一加 15 的长期真机验证仍需安装后确认，不把主机测试写作真机结果。应用内置字体及网页明确指定的网络字体不属于系统字体入口。
