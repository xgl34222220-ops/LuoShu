# 更新日志

本文件记录洛书的重要功能变化。正式发布版本以 GitHub Releases 中的标签、构建产物和 SHA-256 为准。

## [Unreleased]

- 暂无。

## [v3.3.6] - 2026-08-25

- 恢复直接字体切换的前台快路径：设备对齐缓存未命中时不再同步执行多分区 XML 扫描、九档字重准备或 Python 生成。
- 前台只保留真实槽位硬链接映射、安全快照、负载校验与原子提交；完整设备对齐继续在低优先级后台生成，已有可信缓存仍可立即激活。
- 字形覆盖校验结果改为按字体文件身份分别缓存，并保存 CJK/Latin 能力；切回已验证且未变化的字体不再重复扫描完整 cmap。
- 新增性能回归门禁，明确禁止直接切换的缓存未命中路径进入重量准备、XML 生成或清理回退。
- 保留 v3.3.5 的 36 dp 顶栏按钮修正以及现有事务回滚、重启保护和安全验证。

## [v3.3.5] - 2026-08-25

- 字体组合页右上角工具与刷新按钮统一为 36 dp 可视容器和 18 dp 图标，并保留 48 dp 无障碍触控区，修复刷新按钮明显大一圈的问题。
- 刷新按钮显式使用与工具按钮相同的主题色、圆角和低阴影；Miuix 与 Material 页面不再各自维护不同尺寸的刷新控件。
- 字体组合页标题收紧到 30 sp，避免标题与操作按钮共同挤压窄屏顶栏。
- 保持 v3.3.4 的 v3.0.0 字体运行时不变；直接从 v3.1–v3.3 升级到本版仍会安全清除旧问题负载。

## [v3.3.4] - 2026-08-25

- 字体生成、XML overlay、字体度量、HyperOS 槽位、自挂载和启动回滚运行时完整恢复到 v3.0.0 稳定基线。
- 撤销 v3.1.0 的变量字体实例缓存、v3.2.0 的九档批处理与紧凑行框改造、v3.3.0 的 HyperOS 覆盖下限与切换缓存层。
- 从旧版升级时一次性清除 3.1–3.3 生成的活动字体负载，保留字体选择及组合偏好；首次开机保持系统字体，避免旧负载继续触发无效切换或循环重启。
- 保留 v3.3.3 的紧凑 App 界面和真实挂载错误显示。模块 versionCode 为 `30304`，App versionCode 为 `3030401`。

## [v3.3.3] - 2026-08-24

- 修复 KernelSU 等环境下把缺失的 `system_ext/fonts`、`product/fonts` 等可选字体目标当作致命错误、进而回滚整个字体挂载事务的问题；仅 `system/fonts` 保持必需。
- 自挂载状态、首页与任务中心现在会保留并显示真实失败目标，避免只报告笼统的可见布局差异。
- 全面压缩首页、字体库、任务中心、设置页和底部导航的标题、卡片、图标、间距及按钮尺寸，恢复紧凑的信息密度。
- 补充可选分区缺失、运行时挂载与状态桥接回归测试。模块 versionCode 为 `30303`，App versionCode 为 `3030301`。

## [v2.3.7] - 2026-07-30

- 合入本次启动激活证据门禁：字体事务、自挂载清单、动态字体配置与 `bootId` 必须属于当前启动，只有 PID 1 可见且原子事务为 `mounted` 才显示生效。
- 修复 APatch 通过 `source` 执行 `customize.sh` 时，脚本末尾 `exit 0` 提前终止父安装器、导致模块未提交的问题。
- 字体导入状态改为事件驱动，移除 App 与前台服务的 250ms 常驻轮询，并将服务改为 `START_NOT_STICKY`。
- 高刷新率改为可选项，默认跟随系统；省电模式下即使已开启也不会强制高刷新率。
- 首页与设备可信状态统一显示“已准备、等待完整重启、本次启动已验证”以及动态配置、自挂载等具体失败原因。
- 自挂载验证增加 PID 1 `mountinfo` 目标核对；逐文件 bind 还要求挂载源与可见目标具有相同设备号和 inode。
- 发布 APK 固定校验 v2.3.6 正式证书 SHA-256 和单 signer；稳定版必须有 ColorOS、HyperOS、Magisk、APatch 四类真机验收证据。
- 模块预发行 versionCode 为 `20307`，App versionCode 为 `2030701`。

## [v2.3.6] - 2026-07-29

- 修复字体任务在完整重启后因 PID 复用或遗留状态被误判为仍在运行的问题，后台任务身份增加 Android `bootId`。
- 字体切换增加心跳、统一超时状态与更长的 App 恢复窗口，改善大型 CJK、TTC、CFF/CFF2 和复杂可变字体处理稳定性。
- 字体库指纹查询不再隐式启动槽位规划；规划任务加入超时、低优先级、轻量缓存键、双重缓存检查和陈旧锁恢复。
- 取消开机自动深度字体加载验证，不再重复遍历、哈希完整字体树或启动 Python；正常开机只读取已确认事务与自挂载状态。
- 深度加载验证保留为显式 `device_font_load_verify.sh verify` 诊断命令。
- 修复任务中心诊断与导出按钮对齐，并完善 App 自动安装重试分类。
- 模块正式版 versionCode 为 `20306`，App versionCode 为 `2030601`。

## [v2.3.5] - 2026-07-26

- 正式收敛 Android 16、KernelSU、Magisk 与 APatch 的无元模块字体挂载路径。
- 修复 OEM 分区遗漏、跨 ROM 缺失别名、符号链接共享真实目标及非核心附加组件零 bind 目标造成的回滚。
- 严格自挂载守卫确认全部必需文件均在 PID 1 根命名空间可见后，记录为 `verified + mount-verified`，不再把已经实际显示的字体误报为“待验证”。
- 首页、真机验收与发布门禁统一接受可靠挂载证据；缺失、部分挂载或可见性不一致仍保持失败并完整回滚。
- 同 versionCode 的 Alpha5 App 会按 APK SHA-256 识别正式构建并执行一次覆盖更新。
- 模块正式版 versionCode 为 `20305`，App versionCode 为 `2030501`。

## [v2.3.5 Alpha5] - 2026-07-26

- 修复兼容 bind 已成功接管字体后，`system/etc` 仅含本机不存在的挂载探针或动态配置时被误判为 `bind-incomplete` 并回滚的问题。
- bind 回退区分“实际挂载失败”与“没有任何本机可替换目标”；非核心零目标组件安全跳过且不进入必需清单。
- `system/fonts` 仍必须至少挂载一个真实字体目标，实际 bind 或可见性失败仍保持完整原子回滚。
- 增加字体组件成功、附加配置组件零目标的回归测试，并继续验证零字体目标必须失败。

## [v2.3.4 Alpha4] - 2026-07-26

- 修复 Android 16 与部分 OEM ROM 中多个字体名通过符号链接共享同一真实文件时，逐文件 bind 相互覆盖并触发 `system/fonts-visibility-mismatch` 回滚的问题。
- bind 阶段优先处理真实字体文件，并按解析后的真实挂载目标去重符号链接别名；验证阶段使用相同顺序与目标集合。
- 增加两个不同负载名指向同一 ROM 字体目标的回归夹具，确认规范目标不会被后续别名覆盖。

## [v2.3.3 Alpha3] - 2026-07-26

- 修复 Android 16 / KernelSU 上只读 OverlayFS 失败后，逐文件 bind 因模块包含本机不存在的跨 ROM 字体别名而被误判为不完整，进而回滚所有已成功字体挂载的问题。
- bind 回退只对当前 ROM 中真实存在的文件执行替换；每个字体或配置组件仍必须至少挂载一个真实目标，实际 bind 或可见性验证失败仍执行完整原子回滚。
- 首页“挂载引擎”状态改为读取真实自挂载结果，不再把“模块已安装”误显示成“挂载正常”。
- 增加“存在可替换字体并带有本机缺失别名”成功用例，以及“没有任何兼容目标”必须失败的回归测试。

## [v2.3.2 Alpha2] - 2026-07-26

- 修复旧版本升级时遗漏 `oplus_*`、`my_*`、`mi_ext`、`cust`、`hw_product` 等 OEM 分区字体负载，导致部分 ColorOS、OxygenOS、realme UI、MIUI/HyperOS 和华为系设备回退默认字体的问题。
- 修正 APatch 自挂载生命周期，改为在 OverlayFS 就绪后的 `post-mount` 阶段挂载字体负载。
- 升级时不再迁移上一次开机的挂载结果和设备字体验证证据，避免把过期成功状态误当成本次已生效。
- App、Root 管理器描述和任务中心区分“已配置字体”与“设备实际生效字体”；挂载失败或验证失败时明确显示系统默认字体。
- 增加 OEM 分区迁移、APatch 生命周期、自挂载幂等、状态桥接和首页展示回归测试，并将关键门禁接入候选版 CI。

## [v2.3.0] - 2026-07-26

- 将真实字体负载迁移到 `.luoshu-payload/<partition>` 私有目录。
- 标准 `system`、`system_ext`、`product`、`my_product`、`vendor` 模块目录只保留空壳。
- 生产路径固定使用洛书自挂载，不再检测、配置、同步或依赖任何元模块。
- KernelSU / SukiSU Ultra 在 `post-mount` 阶段自挂载，Magisk / APatch 在 `post-fs-data` 阶段自挂载。
- 保留 ROM 原厂 Emoji、图标、衬线、斜体、fallback 与未被洛书管理的字体槽。
- 重复执行不叠加第二层挂载；失败保持 fail-open，不阻断系统启动。
- 卸载时逆序解除洛书记录的挂载，并清理私有负载和自挂载状态。
- 刷入界面、README、完整使用教程和测试矩阵删除元模块推荐及白名单引导。
- 模块版本为 `v2.3.0 / 20300`，正式 App `versionCode` 为 `2030001`。

## [v2.2.9] - 2026-07-25

- 尝试修复 Mountify 与 Magic Mount RC/RS 的直接源兼容和探针回滚问题。
- 该兼容方向已由 v2.3.0 私有自挂载架构取代。

## [v2.2.8] - 2026-07-25

- 恢复部分直接源元模块兼容逻辑。
- 该版本的外部元模块适配逻辑不再用于 v2.3.0。

## [v2.2.7] - 2026-07-25

- 修复卸载脚本重新创建日志目录和模块残留的问题。
- 清理洛书生成的挂载状态、锁、备份与临时文件，同时保留非洛书数据。
- 新增卸载回归测试。

## [v2.2.4] - 2026-07-25

- 修复恢复系统字体后首页仍提示“设备字体尚未验证”。
- 区分系统字体、设备负载已验证、兼容映射、等待验证与异常状态。
- 增加开机加载验证延迟和重试。
- 修复 MIUIx 任务中心诊断与刷新按钮对齐。

## [v2.2.3] - 2026-07-25

- 完整重启后自动执行设备字体加载验证。
- 单字体切换改为独立 Root Worker。
- 安全超时提升到 110 秒并保留事务回滚。

## [v2.2.2] - 2026-07-25

- 字体详情增加格式、SHA-256、TTC 字体面、字重、覆盖和可变轴分析。
- 字体工坊增加组合方案、字形浏览和备份恢复能力。
- 构建改为单包 App-only 模式，模块始终内置正式 App。

## [v2.2.1] - 2026-07-24

- 大型字体别名与负载槽位改用快速文件大小读取。
- 完整验证结果按文件身份缓存。
- 增加字体切换分阶段性能日志。

## [v2.0.0] - 2026-07-21

- 重构为无 Hook 全局字体引擎。
- 在 Zygote 启动前生成 systemless 字体覆盖，不直接修改只读系统分区。
- 支持设备字体清单、复合字体、真实字重、可变轴、事务提交与失败回滚。
- 保留 Emoji、Symbol、图标、monospace、serif、数学和专用字体。

更早的开发历史保留在 Git 提交、历史标签和对应 GitHub Release 中。
