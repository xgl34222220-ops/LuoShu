# Changelog

公开版本历史从重构系列重新开始。

## [2.0.0] - 2026-09-22

- 新增「字体覆盖」中心：按槽位展示已替换、待验证、系统保护、异常与可安全补齐状态。
- 刷写阶段与 App 改为同一份设备字体 inventory / census 口径，避免槽位数量各算各的。
- 「一键补齐」使用精确 remediation plan，并在新 stage 中回填完整安全字体树；重启后按真实挂载与加载证据验证。
- OEM 嵌套字体根（如厂商自定义 `/product/.../fonts`）进入真实 Overlay/Bind 挂载，不再出现“扫描到了但没有挂进去”。
- 修复组合任务残留 PID 被 Android 复用后误报“已有组合任务正在运行”。
- 修复复合字体兼容运行时软链接目录被 `mkdir -p` 误判为工作目录创建失败。
- 补齐任务在 App 中直接接管真实 taskId，实时显示阶段、进度和失败原因；成功后直接进入完整重启状态。
- 组合补齐提交改为 generation-bound 原子事务；后台 finalizer 丢失时可自动恢复，不再永久停在 99%。
- 优化补齐末段性能：ROM 槽位度量、批量补齐前移到 90–98%，已规范化槽位不再重复跑 FontTools，99% 只保留轻量原子提交。
- 下一正式版为 2.2.2。

## [1.1.1] - 2026-09-20

- 安装阶段原厂字体清单改为 fresh stock scan，修复 ColorOS 旧槽位污染导致的重扫失败。
- 增加未知 OEM 字体分区自动发现，并贯穿扫描、payload、自挂载和运行时映射。
- 字体切换加入本机 metrics 对齐缓存、字形校验缓存与低优先级后台预热。
- 快速切换缓存加入数量与总容量双限制，避免模块目录异常膨胀。
- 清理重复扫描器、legacy 重复 helper、一次性发布脚本与重复文档。
- 内部启动核心整理到 `.luoshu-runtime/core/` 与 `.luoshu-runtime/compat/`。
- App 正式启用九宫格鎏金版洛书新图标。
- 下一正式版为 2.0.0。

## [1.0.0] - 2026-09-13

- 洛书重构系列首个正式版本。
- 中文、英文、数字字体支持独立选择与复合生成。
- 设备字体清单、OEM 字体路由、事务提交、缓存复用与安全回退整合为统一字体引擎。
- 完善 HyperOS / ColorOS 字体覆盖与英文数字路由。
- 设置中加入中文“Google 字体兼容”页面，支持状态检测、开启与恢复。
- 正式版本序列从 1.0.0 开始，下一正式版为 1.1.1。
