# 洛书 v2.3.5

v2.3.5 是经过多轮 Android 16 与 KernelSU 真机验证后的正式稳定版。它保留洛书无 Hook、无元模块的全局字体架构，并修复部分 OEM ROM 上字体回退系统默认、挂载事务被误回滚，以及字体已经实际显示却仍提示“待验证”的问题。

## 主要修复

- 完善 `oplus_*`、`my_*`、`mi_ext`、`cust` 等 OEM 分区字体负载迁移。
- KernelSU / SukiSU Ultra 在 `post-mount` 阶段恢复字体自挂载，Magisk / APatch 保持早期挂载路径。
- 逐文件 bind 仅替换当前 ROM 中真实存在的目标，安全跳过其他 ROM 才存在的字体别名。
- ROM 多个字体名指向同一真实文件时，按真实目标去重并优先处理实体文件，避免后续别名覆盖。
- 非核心 `fonts` / `etc` 组件没有本机可替换目标时安全跳过，不再回滚已经验证的核心字体挂载。
- `system/fonts` 仍必须至少接管一个真实字体文件；任意实际 bind、记录或可见性失败都会完整回滚。
- 兼容字体负载通过严格自挂载守卫，并确认全部必需文件在 PID 1 根命名空间可见后，记录为 `verified + mount-verified`。
- 首页、真机验收与发布门禁统一显示真实生效状态，不再把已经生效的字体永久标记为“待验证”。

## 安装

1. 在 KernelSU、SukiSU Ultra、Magisk 或 APatch 中直接刷入模块 ZIP，不需要安装任何元模块。
2. 完整重启设备。
3. 首次打开洛书时允许 Root 授权。
4. 选择单字体或组合字体，任务完成后按提示完整重启。
5. 首页显示“设备字体挂载已验证”即代表当前字体负载已经通过系统可见性校验。

模块 ZIP 已内置同版本正式签名 App；也可以单独安装发布页中的 APK。覆盖更新会保留字体库、组合方案和现有配置。
从 Alpha5 覆盖正式版时，模块会比较内置 APK 的 SHA-256；即使 versionCode 相同，也会将 Alpha5 App 更新为正式构建。

## 安全约束

- 不修改只读系统分区中的原始字体文件。
- 字体挂载事务要么全部提交，要么完整回滚，不保留 ROM 与洛书混合的半成品字体树。
- 挂载失败时安全使用系统默认字体，并在任务中心保留诊断信息。
- 卸载模块后可恢复系统原始字体状态。

## 版本信息

- 模块版本：`v2.3.5`
- 模块 versionCode：`20305`
- App versionName：`2.3.5`
- App versionCode：`2030501`
- 发布类型：正式稳定版
