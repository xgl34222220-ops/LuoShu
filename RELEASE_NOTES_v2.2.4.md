# 洛书 v2.2.4

洛书 v2.2.4 是针对 v2.2.3 真机反馈的状态与界面修复版本，重点解决恢复系统字体后仍显示“设备字体尚未验证”，以及任务中心右上角按钮依旧错位的问题。

## 主要修复

- 首页可信状态不再只依赖旧验证文件，同时读取当前实际字体状态。
- 恢复系统字体后明确显示“当前为系统字体，无需进行加载验证”，不会再要求用户重复应用或重启。
- 明确区分系统字体、设备专属负载已验证、兼容字体映射、等待验证和验证异常五类状态。
- 兼容映射不再伪装为“等待重启验证”，并明确说明该字体没有设备专属加载证据。
- 可信状态详情增加当前字体类型和真实验证原因，便于判断是等待重启、后台任务未完成还是负载清单异常。
- 开机验证等待窗口延长，并对瞬态状态自动重试；`late_start service` 在模板、索引与负载维护完成后再次执行最终验证。
- MIUIx 任务中心的诊断与刷新按钮改为放入同一个 `Row`，使用相同的 56 dp 尺寸和共同垂直中线，不再依赖真机不稳定的固定顶部偏移。
- 保留 v2.2.3 的独立 Root Worker、110 秒安全超时、完整后台验证和失败事务回滚。

## 安装与升级

1. 在 Magisk、KernelSU、SukiSU Ultra 或 APatch 中刷入 `LuoShu-v2.2.4.zip`。
2. 完整重启设备。
3. 打开洛书并授予 Root 权限；恢复系统字体时首页应直接显示无需验证，使用洛书字体时显示真实加载状态。

模块 ZIP 内置同版本正式签名 App。独立的 `LuoShu-App-v2.2.4.apk` 可用于单独安装或覆盖升级。

## 版本信息

- 模块版本：`v2.2.4`
- 模块 versionCode：`20204`
- App versionCode：`2020401`
- 最低 Android：Android 9 / API 28
- 目标 Android：API 36

## 校验

Release 同时提供模块 ZIP、正式 APK 及各自的 SHA-256 文件。下载后可使用对应 `.sha256` 文件核验完整性。
