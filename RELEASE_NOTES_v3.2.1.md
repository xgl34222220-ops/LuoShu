# 洛书 v3.2.1

洛书 3.2.1 是基于 v3.2.0 的稳定性、安全与可恢复性更新，重点加固 Play/GMS 字体 provider 重启验证、字体事务异常恢复、第三方字体导入和复合字体生成。字体覆盖仍保持无 Hook 方案。本版本按正式稳定版发布。

## Provider 与字体缓存验证
- 字体切换后主动失效字体引擎、系统字体服务和 Google/Play 字体 provider 的缓存，降低旧字体对象继续驻留导致的局部不生效。
- 对相关 provider/zygote 执行受控重启后重新扫描进程，并验证新进程与目标包一致；无法确认时保留明确失败状态，不把“已发出重启请求”误报为“已验证”。
- 扩充 ColorOS、HyperOS 与 Google 字体 provider 的识别和验证回归，覆盖重启成功、超时、PID 复用与缓存失效路径。

## 字体事务恢复
- payload 两阶段提交增加持久事务状态文件，记录 staging、备份、激活和完成阶段，不再只依赖目录是否存在来猜测恢复动作。
- 增加事务外层快照；进程被强杀、OOM 或重启打断时，下一次执行会按状态机恢复最后一个完整负载。
- 恢复失败保持 fail-open 并输出可诊断状态，避免删除仍可用的备份或把半成品标记为成功。

## 导入与合成安全
- ZIP 导入在解压后检查规范路径碰撞、文件数量、单文件大小和总解压大小，拒绝异常归档和膨胀负载。
- 复合字体生成在异常路径显式关闭已加载字体对象，降低低内存设备上的残留内存峰值。
- 字重匹配增加 `OS/2` 之外的名称与样式回退；基准线探针离散度异常时回退安全对齐，减少艺术字体或错误元数据导致的错位。

## 构建链与合规
- 内置 CPython、FontTools、Android NDK 与最低 Android API 改为单一版本清单锁定，所有构建工作流共用同一来源并生成运行时版本记录。
- 增加 Dependabot 与定时 FontTools 安全审计，补充 GPL 分发边界和内置运行时依赖说明。
- 新增事务中断恢复、ZIP 限额、复合字体保护、provider 重启验证和缓存失效行为测试，并接入常驻源码检查。
- v3.2.1 正式发布工作流会重新执行完整源码检查、Android Release Lint、单元测试、固定签名 APK 构建、证书 SHA-256 校验、模块 ZIP/App 字节一致性检查以及发布就绪门禁。

## 版本信息
- 模块版本：v3.2.1
- 模块 versionCode：30201
- App versionCode：3020101

## 发布验证
ColorOS 16、HyperOS 3、Magisk、APatch 的完整真机设备矩阵仍保持 pending；本次稳定版由维护者明确授权放行，不伪造未执行的真机 PASS。

<!-- release-trigger: v3.2.1 -->
