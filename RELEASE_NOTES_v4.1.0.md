# 洛书 v4.1.0 正式版

本版修复 HyperOS 字体度量与开机覆盖问题，减少应用字体阶段的重复处理，并清理过时运行分支和发布脚本。

## HyperOS 字体显示

- 修复原厂字体度量读出后又被固定比例截断的问题，按物理槽保留 hhea、OS/2 和行距。
- 修复开机补齐时把 product 等分区已经处理好的字体覆盖为 system 版本的问题。
- 相同 hhea、不同 OS/2 的字体槽分别处理，实际多字重来源得到保留。
- 原厂清单缺失或过期时记录回退原因；没有把未知的原厂度量当作已匹配。

## 应用字体与超时

- HyperOS 字体槽在一个 Python 进程内批量处理，同源同合约复用输出；修改度量时不再重编译整个字形表。
- 修复单字体和复合字体忽略 HyperOS 处理错误、继续提交并报告成功的问题。
- 超时信息显示最后执行阶段。默认超时没有加长，当前启动使用的字体仍由独立暂存与下一启动提交保护。

## 运行链路清理

- 复合入口直接交给实际后端，移除未使用的后备实现及重复的前置运行时加载。
- 去掉两条切换链路中重复的全量复制重试，保留硬链接和普通复制两种必要方式。
- 删除已完成使命的一次性旧版本覆盖工作流、旧版本替换分支和失效的 v2 扫描测试。
- 发布继续校验源码、App lint/单元测试、签名、模块结构与校验和；源码全量检查由打包脚本统一执行一次，去除流水线里的重复执行。

## 安装

1. 在 Root 管理器安装 `LuoShu-v4.1.0.zip`，按提示完整重启。
2. 打开洛书，重新应用一次所需字体，等待完成后完整重启，使新的度量处理生效。

模块版本代码 **40100**，App 版本代码 **4010001**，可识别从旧 v4.0.0 升级。

本版完成源码及自动化回归；ColorOS/HyperOS 真机矩阵仍待补齐。不能据宿主机性能测试保证所有手机一分钟内完成，也不声称任意字体在所有 App 中都已完全对齐。仍有偏移或失败时，请导出洛书日志并附上机型、系统版本及字体名称。
